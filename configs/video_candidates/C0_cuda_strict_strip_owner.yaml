# Candidate-only first v2 data plane.  This declaration deliberately has no
# C1--C8 visual component: it is an auditable CUDA calibration + strict owner
# experiment, not a shortcut that relabels the legacy visual seam as CUDA.
config_schema: gemini305-video-candidate/v1
role: candidate
candidate_id: C0_cuda_strict_strip_owner
parent_candidate_id: C0_baseline_reference
algorithm_id: C0_cuda_strict_strip_owner
implementation_id: torch_cuda_strip_owner_v2
model_sha256: {}
allow_baseline_fallback: false
changed_components: [cuda_calibration_and_strict_owner_data_plane]
required_evidence_components: [orb_anchor_trajectory]
required_output_components: [c1_constrained_owner]
replaces_output_components: [legacy_visual_seam]
components:
  seam: {mode: hard_owner}
  flow: {backend: none}
  mesh: {enabled: false}
  object_lock: {enabled: false}
  multiband: {enabled: false}
  photometric: {enabled: false}
config_sha256: fbde6e1f34661068c043c387466438974310d218b3300abbb7d41595d3d479de
