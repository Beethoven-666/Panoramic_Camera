# 修改方案 v5：全内容统一中央条带与无重影定位

> 版本：v5.0  
> 日期：2026-07-28  
> 状态：最新实施方案，替代 v4 的物体世界层主路线  
> 当前强回归会话：`data/captures/run_20260727_110952_326`  
> 当前问题输出：`D:\central_strip_Panoramic_Camera\new_outputs\run_20260727_110952_326_v4_r1c6`  
> 目标：消除物体重影、恢复正确位置，并使物体与背景共享同一套无缝中央条带拼接语义

---

## 0. 最终裁决

v5 不再继续修补 v4 的物体世界层、物体 2.5D mesh、object-rich corridor 或独立物体 owner 覆盖。正式 RGB 全景改为：

```text
严格同步 RGB-D、标定与原始轨迹
→ 可审计的轨迹优化候选
→ 单一连续扫描坐标
→ 所有真实 RGB 帧的统一中央条带 inverse remap
→ 相邻真实条带的统一 RGB 对应和局部对齐候选
→ 一张全内容单调 owner 图
→ 保留现有背景 GraphCut / 光度 / 局部 MultiBand 内核
→ valid-mask 裁切、重影/位置/接缝审计
→ 原子发布
```

核心语义固定为：

1. 背景、货架、纸箱、充电器、风扇、线缆、透明或反光物体都使用同一个扫描坐标、同一种中央条带采样和同一张最终 owner 图。
2. 不再把物体投影到深度世界层、参考深度平面、物体 spatial panel 或单独 target footprint。
3. 不再在背景合成前挖掉物体区域，也不再在背景完成后写入物体 patch、矩形 corridor、inverse mesh owner 或 world-anchor overlay。
4. aligned depth 仍可用于轨迹、遮挡风险、对应拒绝和质量审计，但不得决定物体最终目标位置，不得为 RGB 生成物体世界 footprint。
5. 最终像素只能来自真实 RGB 帧；不以参考图、旧全景、TSDF 或生成模型补色。
6. 取消总耗时小于 300 秒的限制。耗时、峰值内存、显存、H2D/D2H、后端和模型版本仍必须完整记录。

---

## 1. 对 `v4_r1c6` 的事实诊断

### 1.1 当前正式输出并不是真正的 fixed-strip pushbroom

`inspection_meta.json` 明确记录：

```text
fixed_strip_pushbroom = false
backend = overlapping_virtual_perspective_panels_rgbd
layout.model = overlapping_straightened_virtual_perspective_panels
panel_count = 12
reference_depth_mm = 976.2999877929688
real_pose_count = 101
selected_full_fov_source_count = 12
```

因此，当前正式链实际是“101 个真实 pose 中选择 12 个全 FOV 虚拟透视 panel，再以参考深度和物体专用规则合成”，不是“101 帧全部走统一中央窄条”。

用户要求保留的优秀背景效果，实际主要来自以下成熟内核：

- 相邻 panel 的单调链拓扑；
- adaptive nominal boundary；
- GraphCut seam；
- 连续光度校正；
- 安全局部 MultiBand；
- owner topology 和 valid coverage 审计。

v5 保留这套背景接缝与光度内核，不保留其“参考深度平面 + 独立物体世界层”的双重几何。

### 1.2 双重几何是物体位置偏移的直接来源

当前输出同时存在：

```text
reference_plane_fallback_owner_pixel_count = 1,631,269
reliable_depth_owner_pixel_count = 6,851
```

绝大多数像素按参考平面语义落位，少量物体像素又按深度 mesh、identity inverse mesh 或 corridor 语义写入。两种坐标系对近物体产生不同的目标位置和尺度，最终会形成：

- 物体相对货架位置漂移；
- 同一物体的背景底图轮廓和物体专用轮廓并存；
- 物体边缘附近出现局部拉伸、剪纸边和背景泄漏；
- 矩形或不规则 owner 区域与背景形成明显边界。

### 1.3 双重 owner 是重影和硬边的直接来源

`object_tracks.json` 中 19 个 required track 的最终 disposition 为：

```text
1 × DIRECT_RGBD_WORLD_OWNER
18 × OBJECT_RICH_SINGLE_SOURCE_CORRIDOR
```

同时：

```text
pre_seam_hard_owner_interval_count = 11
post_background_hard_owner_count = 10
accepted_inverse_mesh_owner_count = 1
```

这意味着背景单调 owner 图之外，又存在物体 hard interval、post-background owner 和 inverse mesh。即使每个分支内部审计通过，分支之间仍可能对同一真实内容给出不同位置、范围和边界。

### 1.4 当前报告已经暴露这一结构问题

`delivery.json` 的严格失败原因包含：

```text
pre_seam_single_panel_hard_owner_interval_used
foreground_identity_single_owner_inverse_mesh_used
background_owner_boundary_has_visible_color_discontinuity
foreground_object_lock_rejected_by_closed_seam_topology
multiview_observed_near_world_surface_missing_from_final_rgb_owner
```

此外：

```text
owner boundary Lab P95 = 33.301651
maximum allowed P95 = 30
multiview world coverage ratio = 0.455122853
required ratio = 0.8
```

这些不是继续增加物体 mesh、track 或覆盖回解即可稳定解决的问题。根因是正式 RGB 同时受两套位置模型和多套合成入口控制。

### 1.5 视觉证据

当前全景可见：

- 左中部黑色充电器出现重复和不连续轮廓；
- 中部纸箱及其下方物品发生水平错位、截断和背景泄漏；
- 风扇右侧小物件存在硬边和局部位置跳变；
- 多处边界呈矩形或沿 object corridor 截断，而不是自然的内容接缝；
- 背景墙、横梁和立柱整体接缝明显优于物体区域。

因此 v5 的正确方向是保留背景接缝内核，删除物体专用坐标与二次合成。

---

## 2. v5 的保留项、删除项和新授权

### 2.1 保留

保留并扩展 v4 中以下有效成果：

- 严格 RGB-D 会话、标定、曝光、时间戳和原子交付；
- ORB-SLAM3、Open3D 和 pose provenance；
- `VisibilityMatrix`，但仅用于候选选择、风险和验收；
- `PixelProvenance`，扩展为每个最终像素的真实来源记录；
- 全局或分段光度图；
- 相邻链 GraphCut、单调 owner topology 和局部 MultiBand；
- BackendRegistry、模型版本、SHA256、实际 CUDA provider 和消融记录；
- train/held-out、forward/backward、Jacobian、直线和尺度审计；
- object inventory、薄结构和 reference 图，仅作为验收证据；
- A/B/C/F、`delivery_state` 和人工复核语义；
- TSDF 只作为独立 3D 浏览产品，不影响 RGB。

### 2.2 从正式 RGB 路径删除

以下能力可保留为诊断或历史消融，但不得再写 `panorama.jpg`：

```text
DIRECT_RGBD_WORLD_OWNER
LOCAL_25D_SINGLE_RGB_OWNER
OBJECT_RICH_SINGLE_SOURCE_CORRIDOR 作为独立物体渲染模式
foreground_world_anchor
foreground_object_anchor
identity owner inverse mesh
object target footprint
object spatial_panel_index 决定 RGB 位置
object footprint 从背景标签域挖除
post-background object overlay
reference-plane object replacement
以 z-buffer 决定物体 RGB 目标坐标
以 object mask 矩形/走廊改写最终 owner
```

对象检测、FastSAM、DIS track、OCR、深度组件仍可用于“不要在此处切 seam”“此区域需更强对应”“验收中是否丢失/重复”，但不再拥有独立渲染权。

### 2.3 本版本明确采用的用户新授权

| 旧约束 | v5 处理 |
|---|---|
| 前景实例仅限 owner-only | 取消。前景与背景统一参加候选 owner、局部 warp 和安全融合；不可靠时仍可自适应坍缩到单源 |
| 不改写/插值真实 ORB-SLAM3 pose | 取消不可变限制。保留 raw pose，允许有界优化和区间内连续时间插值；不得覆盖 provenance |
| 禁止 UniStitch/LightGlue/MAGSAC/Torch | 取消。全部进入可关闭、可消融的候选注册表 |
| 禁止全局或 pose 级 3×3 单应 | 取消。允许作为受审计候选；物体丰富或明显非平面场景中不会因“可用”而自动成为默认 |
| `local_apap_flow` 默认关闭 | 取消。改为 `auto`，由 held-out 证据逐 pair/区段启用 |
| 总耗时小于 300 秒 | 取消。只记录，不以 300 秒拒绝结果 |

这些能力是“允许参与竞争”，不是“必须叠加”。默认选择最低复杂度且通过盲测的模型。

---

## 3. 目标架构：Unified Central Strip v1

建议正式后端命名：

```text
unified_calibrated_central_strip/v1
```

建议正式 delivery schema 升级为：

```text
gemini305-panorama-delivery/v12
```

完整数据流：

```text
严格 RGB-D 会话
→ raw ORB-SLAM3 / Open3D / 可选特征因子
→ PoseRegistry(raw, optimized, covariance, delta)
→ ScanPath 一维连续扫描坐标
→ 所有真实帧的一次标定 inverse remap
→ content-agnostic central strips
→ adjacent-strip correspondence candidates
→ minimum-complexity validated warp selector
→ 统一全内容 owner/seam
→ 保留背景光度和局部 MultiBand 内核
→ valid-mask 最大内接裁切
→ ghost / position / seam / background-regression audit
→ RGB provenance、报告与原子发布
```

系统中只允许存在一张最终目标坐标图、一张有效 mask 和一张 owner/contributor 图。物体检测结果不得创建第二张 RGB 画布或第二个合成阶段。

---

## 4. 单一扫描坐标与物体位置

### 4.1 ScanPath

对相机中心建立连续扫描路径：

```text
s(t) = 相机中心沿审计后扫描主曲线的弧长
x_canvas = a × s(t) + b
```

近似直线扫描时，`s(t)` 退化为相机中心在主扫描轴上的投影；轻微弯曲、非匀速扫描时使用单调弧长。所有内容共享该 `x_canvas`，不得按深度或 object centroid 另算目标 x。

目标 y 来自统一校平的虚拟相机坐标和真实 RGB 像素，不按物体深度单独移动。

### 4.2 位置为何不再依赖物体世界层

真正的侧扫中央狭缝中，一个静态场景点在相机经过其中央视线时被记录。其全景横坐标首先由扫描时刻和相机沿轨迹的位置决定，而不是由该点被投影到某个背景参考平面或物体局部 mesh 的结果决定。

因此 v5 采用：

- pose/时间决定条带在全景中的顺序和中心；
- RGB 对应决定相邻条带的亚像素残差；
- 深度只帮助拒绝跨遮挡的错误对应；
- object track 不再搬动物体。

### 4.3 PoseRegistry

同时保存：

```text
pose_raw.json
pose_optimized.json
pose_covariance.json
pose_delta.json
pose_source.json
continuous_scan_path.json
```

允许的 pose 候选因子：

- ORB-SLAM3 raw prior；
- Open3D RGB-D odometry；
- LightGlue/SuperPoint/ALIKED + MAGSAC++ E/F/PnP；
- RGB-D point-to-plane；
- 货架直线、结构平面和重力/竖直方向；
- 速度与角速度平滑软约束；
- 可验证长基线和回环。

门禁：

1. raw pose 永不覆盖；
2. 只在真实观测区间内插值，不做首尾外推；
3. 优化增量按位置、旋转、速度和相邻加速度设物理上界；
4. leave-one-edge-out 和 held-out 对应必须改善；
5. 优化后背景回归不得退化；
6. 优化失败时退回 raw，不允许半套 pose 混用；
7. RGB 仍采自真实帧，连续 pose 不代表生成中间 RGB。

### 4.4 位置审计

不依赖参考图的主审计：

- 同一静态结构的多帧中央穿越位置在统一扫描坐标中应收敛；
- held-out 特征投到目标条带后的 x/y 残差；
- 物体中心、轮廓关键点、文字角点、线缆端点的跨源离散度；
- 货架层板与物体接触关系、上下层顺序和遮挡顺序；
- raw pose 与 optimized pose 两种渲染的相对位置对比。

参考图仅作为附加验收，不参与 pose、warp、owner 或最终 RGB。

---

## 5. 全内容中央条带生成

### 5.1 基础实现选择

真正适合改造为正式统一链的是：

```text
calibrated_rgb_pushbroom.py
```

原因：

- 已以真实 pose 建立连续 owner interval；
- 支持 2–160 个真实 pose nodes；
- 中间源只保留中央窄条；
- 首尾只向扫描外侧扩展；
- 每个真实 RGB 源只做一次标定 inverse remap；
- 不以参考深度平面决定条带位置。

`central_strip.py` 当前是 reference-plane diagnostic，仍依赖实测背景平面且正式只允许有限渲染源，不应直接升格为 v5 正式后端。

### 5.2 使用全部真实帧

不再从 101 帧固定选 12 个全 FOV panel。默认保留主扫描段全部真实帧，并按 ScanPath 建立中央条带。

允许为资源或近重复 pose 抑制独立 owner interval，但必须：

- 保留其 raw/optimized pose；
- 保留一次真实 RGB remap 和审计；
- 不生成虚拟 RGB；
- 由相邻真实条带覆盖；
- 抑制逻辑不依赖物体类别或当前样本 frame id。

### 5.3 自适应条带支持域

每帧拥有：

```text
owner core
left overlap support
right overlap support
```

owner core 由相邻扫描位置的中点或连续 Voronoi 分区决定；overlap support 只为对应、seam 和局部融合提供候选。

条带宽度由以下通用量自适应：

- 相邻真实 pose 间距；
- 实际 RGB 局部运动；
- 标定后的有效 FOV；
- 模糊、曝光和遮挡风险；
- 相邻条带所需的对应数量；
- 资源预算。

不得因某个物体 bbox 扩展成独立整块 patch。需要更宽上下文时，扩展的是该相邻 pair 的统一 RGB overlap，不是物体专用 target footprint。

### 5.4 单一 RGB remap

每个 source 的最终采样坐标由下列变换一次组合：

```text
统一标定 inverse remap
∘ 选中的条带局部 inverse alignment
```

不得先生成背景，再二次采样物体。不得先按参考平面采样，再按世界 mesh 重采样同一物体。

---

## 6. 相邻条带对应与模型竞争

### 6.1 候选注册表

每个相邻 pair 或连续局部区段按复杂度生成候选：

```text
C0  identity / 现有背景标定映射
C1  translation / similarity / affine
C2  MAGSAC++ homography 或 pose-level 3×3 候选
C3  local APAP / 规则网格
C4  DIS / RAFT / 其它双向 dense flow
C5  UniStitch 或其它 learned warp/layout challenger
```

选择顺序不是固定流水线，而是最低复杂度优先的竞争：

1. C0 通过则不启用更复杂候选；
2. C0 不通过才评估 C1；
3. 仅在 held-out 改善明确时进入 C2–C5；
4. 任一候选失败都回到上一个安全候选或 hard seam；
5. 不允许把多个未独立通过的 warp 累计叠加。

### 6.2 对应来源

允许组合：

- ORB/SIFT；
- LightGlue + SuperPoint/ALIKED；
- MAGSAC++；
- DIS/RAFT 双向 flow；
- 直线、角点、文字角点和轮廓；
- 深度一致区域的 RGB 对应；
- 可选 UniStitch 中间对应或布局建议。

FastSAM/OCR/object track 可以提高某些区域的验证权重或阻止 seam 切断完整结构，但不能创建独立几何层。

### 6.3 深度在此阶段的合法作用

aligned depth 只可用于：

- 拒绝跨深度断层的错误 feature/flow；
- 标记遮挡、disocclusion、透明和无效深度风险；
- 对 pose 因子加权；
- 判断 homography 是否误把非平面区压平；
- 检查局部 warp 是否改变遮挡顺序；
- 质量审计。

aligned depth 不可用于：

- 把物体 back-project 到世界后再决定最终 RGB 位置；
- 构造物体 target footprint；
- 对物体做 z-buffer RGB 覆盖；
- 生成或补全 RGB；
- 以深度世界层覆盖中央条带结果。

### 6.4 train/held-out

每个 C1–C5 候选必须：

- 将对应按空间 block、结构类别和时间分组；
- train 与 held-out 严格分离；
- 检查 forward/backward；
- 检查正 Jacobian、无 fold-over、位移/尺度/剪切有界；
- 检查直线、文字、轮廓和薄结构不产生新弯曲；
- 检查遮挡前后关系不反转；
- 在未参与拟合的像素上降低双边缘和残差；
- 记录被拒原因。

### 6.5 单应和 learned warp 的使用原则

用户已允许全局或 pose 级 3×3 单应及 learned candidate，但 object-rich 侧扫通常不满足单一平面。故：

- 单应可用于近平面区段、纯旋转或作为 pose/布局候选；
- 不能因 RANSAC 内点数高就覆盖整个非平面扫描；
- 全局 H 必须同时通过背景、近物体、直线、尺度和 held-out 门；
- UniStitch 只提供候选布局、对应或 inverse coordinates；
- learned OOD、低置信或比物理主链差时拒绝；
- 生成式补色不得进入纪实全景。

---

## 7. 统一 owner、GraphCut 和 MultiBand

### 7.1 保留背景接缝内核

从当前 `inspection_chain_seam.py` 保留：

- 相邻有序链；
- 非重叠 pair corridor；
- adaptive nominal boundary；
- GraphCut path；
- row step 和闭合拓扑；
- hard-cut fallback；
- owner coverage/topology audit。

从当前背景合成保留：

- 安全光度样本；
- 连续全景曝光曲线；
- 相邻 residual gain；
- 局部 MultiBand；
- valid mask 和裁切；
- Lab/梯度接缝审计。

首个实施批次应通过 adapter 复用内核，避免为了支持真实 strips 而复制一份略有差异的 seam solver。

### 7.2 一张全内容 owner 图

最终 owner 图直接对所有有效像素求解：

```text
background + object + thin structure + transparent/specular content
→ same candidate strips
→ same pair costs
→ one monotone owner map
```

禁止：

- `object footprint` 从 owner 域挖除；
- `locked_owner_panel_index` 由 object disposition 强制写入；
- seam 完成后再把物体写回；
- 单独的 `spatial_panel_owner` 与 `rgb_source_owner` 共同控制 RGB；
- 同一 track 由 corridor、mesh、world anchor 多路竞争最终写入。

### 7.3 seam cost

统一 seam cost 由以下证据组成：

- 选中 warp 后的 RGB/Lab 残差；
- 梯度方向和边缘双响应；
- feature/flow held-out 残差；
- 局部清晰度和运动模糊；
- 曝光饱和、噪声；
- 深度断层、遮挡和透明风险；
- 线缆、文字和结构线被切断的代价；
- seam 曲率、时间抖动和相邻行步长；
- owner interval 和 coverage。

对象 mask 只能提高“不要切断完整结构”的代价，不能强制把对象搬到另一坐标或写成矩形 owner。

### 7.4 MultiBand

取消“前景一律不得融合”，改为证据驱动：

- warp 后双向一致、光度一致、非遮挡且无 double-edge 的区域可局部融合；
- 遮挡边、透明边、文字、细线或 held-out 不通过时权重自动坍缩到一个真实 RGB source；
- 不对未对齐的边缘做平均；
- MultiBand 仍限于局部 pair corridor，不做全局多源模糊；
- blending mask 与 contributor weights 写入 provenance。

### 7.5 无安全通道时

若一个完整结构跨 seam 且局部 warp 无法通过：

1. 在同一 pair owner 图中扩大 seam 搜索；
2. 尝试相邻 source 的完整覆盖；
3. 选择一个真实 source 拥有该连通区域；
4. 仍保持该区域属于全局 owner 图，不创建 post-overlay；
5. 无完整真实覆盖则降级或 F。

单源是安全回退，不再是所有前景的固定规则。

---

## 8. 光度一致性

保留当前背景中效果较好的光度链，但将求解和应用域统一到真实中央条带：

```text
暗角/响应预校正
→ 安全背景与同表面纹理的 pair observations
→ 全局稀疏 gain 图
→ 相邻 residual correction
→ 最终单调 tone curve
```

要求：

- 不用 object corridor 边界作为光度分段边界；
- 不用 tight object mask 内背景泄漏像素估计 gain；
- 物体和背景最终应用同一 source-domain photometric transform；
- 可按 RGB channel 求解，但需正则、held-out 和范围门；
- 不允许 post-composition 统一增益掩盖竖带；
- 光度失败时保持真实 RGB 单源并透明降级，不以宽带融合掩盖。

---

## 9. 重影、位置和无缝验收

### 9.1 重影指标

新增 `ghost_audit`：

- 高梯度区域的双边缘距离；
- 同一 held-out feature 在多个 contributor 中的残差分布；
- seam 两侧 edge orientation 一致性；
- flow forward/backward 残差；
- 文字、圆孔、线缆和物体轮廓的重复响应；
- 局部 phase correlation 的次峰比；
- 人工复核热图。

验收阈值应按原始分辨率像素和结构尺度归一化，不以当前样本单独调参。首版建议以跨场景开发集确定固定阈值，再冻结后运行盲测。

### 9.2 位置指标

新增 `position_audit`：

- 跨帧同一特征映射到 panorama 的 P50/P95/max；
- object centroid、轮廓关键点和接触点的 source-to-panorama 离散度；
- 同一物体相对货架层板/立柱的几何关系；
- 左右顺序、上下层和遮挡顺序；
- raw/optimized pose 的位置差；
- 可选 reference 对齐后的物体位置误差，仅作外部验收。

禁止用 reference 图反向修正结果。

### 9.3 接缝指标

新增或保留：

- seam Lab ΔE P50/P95/max；
- 梯度跳变和方向差；
- 2–8 px 融合带的风险交集；
- contributor 权重是否归一；
- zero-weight wedge；
- seam 曲率和不连续；
- owner coverage 和单调性；
- object/thin-structure cut count。

### 9.4 背景不退化

以当前背景优良结果建立 frozen regression：

- 使用相同 raw/optimized pose 和相同安全背景 mask；
- v5 在物体及风险 halo 外的背景不得出现新的竖带、断线或亮度跳变；
- seam solver、光度 solver 和 MultiBand 内核通过黄金测试保持行为一致；
- adapter 引入前后运行相同背景 evidence 时 owner/seam 输出逐像素一致；
- 若 pose 优化改变背景，则必须在 held-out 指标上整体改善，并同时保留 raw-pose A/B 图用于比较。

“背景算法不变”在工程上定义为：背景 seam/photometric/blend 内核及其输入语义不被物体专用规则改写；允许真实 strip adapter 提供新的有序相邻 source evidence。

### 9.5 当前强回归样本的观察区

以下区域只用于验收，不得硬编码到算法：

- 左中部黑色充电器和白色底座；
- 中部纸箱、线缆和下方小物件；
- 风扇及其左右相邻物；
- 右侧纸箱、插头和薄结构；
- 黄色横梁、深色立柱和白墙的所有 seam。

每个区域输出：

```text
baseline crop
v5 crop
owner/source visualization
ghost heatmap
position residual overlay
seam/blend mask
```

---

## 10. 泛化性设计

### 10.1 禁止样本专用逻辑

禁止：

- 固定 frame id；
- 固定 12 panel；
- 固定 `976.3 mm`；
- 固定货架层数、坐标或颜色；
- 固定物体类别；
- 当前 reference 专属 mask、crop、gain 或阈值；
- 为 `run_20260727_110952_326` 单独改 seam；
- 以“当前图看起来正确”替代 held-out 指标。

### 10.2 数据分组

开发、验证和盲测必须按物理会话/场景分组，不能把同一扫描的相邻帧随机拆到 train 和 held-out。

至少覆盖：

- 白墙、货架、温室、车间和杂乱室内；
- 0.3–3 m 深度范围；
- 大纸箱、小电子件、圆形物、重复纹理；
- 黑色、透明、反光和深度孔洞；
- 细线缆、软管、插头和网格；
- 低纹理和高纹理；
- 均匀光、亮度渐变和局部曝光变化；
- 慢速、快速、非匀速和轻微弯曲扫描；
- 偶发动态物体；
- 不同分辨率和帧密度。

### 10.3 Metamorphic 测试

同一输入应满足：

- 改变 chunk size 不改变输出；
- CPU/GPU 允许数值微差但 owner topology 和质量等级一致；
- frame 解码顺序变化不改变捕获时间顺序；
- 插入近重复帧不产生重复物体；
- 删除一个非关键帧只造成局部、可解释变化；
- 禁用复杂候选时安全退回低复杂度结果；
- object detector 开/关不改变目标坐标，只影响 seam 风险和验收信息；
- aligned depth 移除时不得偷偷启用世界投影，只能降低对应/pose 置信度或降级；
- reference 图存在/不存在不改变 panorama RGB。

---

## 11. 报告、provenance 和等级

### 11.1 PixelProvenance v2

每个有效像素记录：

```text
target_xy
owner_or_contributor_count
source_frame_ids[]
source_uv[]
weights[]
scan_coordinate
pose_kind
pose_id
alignment_model
alignment_model_id
photometric_transform_id
blend_pair_id
depth_used_for_rejection_only
```

硬门：

- `generated_pixel_count = 0`；
- `unsupported_pixel_count = 0`；
- 每个有效像素至少一个真实 source；
- source UV 有限且在有效标定域内；
- contributor 权重非负且和为 1；
- 不存在 world-object target footprint 来源；
- 不存在 post-background overlay。

### 11.2 新报告字段

至少增加：

```text
renderer_backend
formal_render_path
code_commit_sha
imported_package_path
effective_config_sha256
pose_registry
scan_path_audit
strip_source_audits
pair_alignment_candidates
selected_alignment_models
unified_owner_topology
ghost_audit
position_audit
seam_visual_audit
background_regression_audit
runtime_profile
disabled_legacy_object_render_paths
```

### 11.3 A/B/C/F

- **A**：raw 或严格验证后的 optimized pose 均可；全内容统一中央条带、最低复杂度对齐、重影/位置/接缝/背景回归全部通过，无人工复核。
- **B**：全部视觉和结构门通过，但至少一个区段使用经完整 held-out 验证的 pose recovery、APAP/flow、单应或 learned candidate。
- **C**：来源、coverage、单一坐标、owner topology 和原子发布完整；存在经审计的 hard seam、局部质量不足或需人工复核，但不得有未解决的明显双轮廓或错误位置。
- **F**：RGB 来源不完整、两套坐标/二次合成重新出现、owner 不闭合、物体明显重复或错位、无真实覆盖、非有限 warp/pose、背景严重退化或原子交付失败。

明显重影和错误位置不能仅因“像素都有 owner”而发布 C。

---

## 12. 代码落点

以实际 v11 代码所在提交为实施基线，主要修改：

| 文件/模块 | v5 职责 |
|---|---|
| `stitch_sequence.py` | 正式链切换到 unified central strip；旧 inspection object renderer 只保留诊断 |
| `calibrated_rgb_pushbroom.py` | 全内容真实中央条带、ScanPath、统一 source remap 和 pair evidence |
| `inspection_chain_seam.py` | 作为复用的有序链 GraphCut/owner 内核；不接受 object disposition 锁 |
| `render.py` | 局部 MultiBand、valid-mask、裁切和通用 seam 审计 |
| 新建 `pose_registry.py` | raw/optimized/interpolated pose、协方差和 provenance |
| 新建 `strip_alignment_registry.py` | C0–C5 对齐候选、held-out 选择和回退 |
| 新建 `unified_strip_audit.py` | ghost、position、seam、background regression |
| `inspection_identity_runtime.py` | 仅输出检测/track/验收证据，不返回 RGB owner 或 target footprint |
| `inspection_identity_mesh.py` | 从正式 RGB 路径移除，保留历史诊断 |
| `foreground_object_anchor.py` | 从正式 RGB 路径移除，保留历史诊断 |
| `inspection_object_rich_corridor.py` | 只提供风险/完整结构证据，不写 corridor RGB |
| `inspection_multiview.py` | 降为历史 v11 对照和消融，不再是默认正式 renderer |
| `configs/demo.yaml` | `unified_central_strip`、候选注册表、auto APAP/flow 和新门限 |
| `tests/` | 单元、合成、真实回归、盲测和 schema |

不建议把 `central_strip.py` 的 reference-plane diagnostic 直接改名后当正式实现，以免重新引入背景平面位置语义。

---

## 13. 实施批次

### R0：锁定真实运行代码和黄金基线

当前主环境：

```text
import panorama_demo
→ C:\Users\zyh68\.codex\worktrees\548d\central_strip_Panoramic_Camera\src\panorama_demo
```

而 `D:\central_strip_Panoramic_Camera` 当前处于不同 commit。实施前必须：

1. 选择唯一代码基线；
2. 记录 branch、commit SHA、import path、CLI path 和 config SHA256；
3. 禁止从另一个 worktree 或旧 editable install 运行；
4. 保存 `v4_r1c6` panorama、背景 mask、owner、seam、gain 和关键 crop 为只读黄金基线；
5. 建立“背景内核不变”测试。

完成门：

```text
CLI path == 目标环境
package import path == 目标 worktree
report code_commit_sha == git HEAD
effective_config_sha256 可复现
```

### R1：删除正式物体二次合成，恢复全内容真实条带

1. 正式非 diagnostic 路径改用 `calibrated_rgb_pushbroom`；
2. 使用全部真实 pose nodes；
3. 不调用 `build_inspection_identity_runtime` 生成 owner；
4. 不向 renderer 传 pre-seam object intervals 或 foreground identity owners；
5. 禁用 world-anchor、identity mesh、post-background owner 和 object replacement；
6. 所有 RGB 内容按统一中央条带输出；
7. 接入现有 background seam/photometric 内核 adapter。

这是最重要的最小闭环。R1 必须先证明当前样本的矩形物体覆盖、重复轮廓和错位明显减少，再进入复杂 warp。

### R2：ScanPath 与 pose 候选

1. 建立 raw pose registry；
2. 支持轻微弯曲和非匀速扫描；
3. 加入 LightGlue/MAGSAC/Open3D/结构因子；
4. 实现有界优化和区间内连续 pose；
5. 输出 raw/optimized A/B；
6. 只有 held-out 和背景回归同时改善才选择 optimized。

### R3：统一局部对齐注册表

1. C0 identity；
2. C1 similarity/affine；
3. C2 homography/pose-level 3×3；
4. C3 auto APAP；
5. C4 DIS/RAFT；
6. C5 UniStitch challenger；
7. 每个候选实现统一 train/held-out、FB、Jacobian、尺度和直线审计；
8. 保存选择理由和拒绝理由。

### R4：全内容 seam 与 blend

1. 将真实 strips 适配到现有 ordered-chain solver；
2. 统一处理背景和物体；
3. 允许安全多 contributor；
4. 对遮挡/未对齐边缘自动坍缩到单源；
5. 删除所有 object footprint carve-out；
6. 删除所有 post-composition object write；
7. 保持背景光度和 MultiBand 内核不变。

### R5：ghost/position/背景回归硬门

1. 实现三个主审计；
2. 输出可视化热图；
3. 当前样本关键区域逐项验收；
4. object inventory 只验证完整性和重复，不影响坐标；
5. 明显重影或错位直接 F。

### R6：跨场景冻结默认值

1. 多场景开发集确定固定归一化门限；
2. 冻结参数和模型；
3. 运行物理会话级盲测；
4. 完成 CPU/GPU、分辨率、帧密度和动态场景消融；
5. 通过后将 `unified_calibrated_central_strip/v1` 设为正式默认；
6. v11 inspection renderer 保留为显式 legacy diagnostic。

---

## 14. 测试计划

### 14.1 单元测试

- ScanPath 单调性、弧长和非匀速；
- raw/optimized pose provenance；
- 中央条带 owner interval 完整覆盖；
- 每源一次 inverse remap；
- 相邻 pair corridor 非重叠；
- C0–C5 统一候选接口；
- train/held-out 严格分离；
- forward/backward、Jacobian 和 fold-over；
- unified owner topology；
- contributor 权重；
- 无 object post-overlay；
- ghost/position 指标；
- valid mask 裁切；
- schema 和原子发布。

### 14.2 合成测试

- 不同深度的前景方块经过中央视线，位置不随背景参考平面变化；
- 前景跨三个以上条带时无重复轮廓；
- 细线缆跨 seam 保持连通；
- 透明/深度孔洞只降低深度置信，不改变 RGB 目标位置；
- 近平面场景 homography 通过；
- 非平面场景全局 H 被拒；
- APAP/flow 改善 held-out 后才接受；
- 错误 flow、fold-over 和遮挡反转被拒；
- 动态物体不混合两个时态；
- 近重复帧不产生双物体。

### 14.3 真实回归

至少包括：

- `run_20260727_110952_326`；
- 2026-07-14/15 已有 101-frame 真实轨迹样本；
- 温室旧样本；
- 高曝光历史失败样本；
- 新采集的物体丰富、透明/反光、薄结构和不同速度样本。

### 14.4 必做消融

```text
raw pose vs optimized pose
12 virtual panels vs all real strips
identity-only vs selected local alignment
APAP off vs auto
flow off vs auto
homography off vs auto
UniStitch off vs challenger
depth risk off vs on
object detector off vs on
hard seam vs safe MultiBand
legacy v11 vs unified v12
```

---

## 15. 性能与资源

不设置总耗时小于 300 秒门槛，但必须记录：

- 总耗时和各阶段耗时；
- 实际 frame/source 数；
- CPU 峰值 RSS；
- GPU 峰值显存；
- H2D/D2H；
- 每个候选后端和实际 provider；
- model path、SHA256 和 warm-up；
- pair 候选数量、接受数量和拒绝数量；
- 中间缓存、chunk 和流式 working set；
- 是否发生 CPU fallback。

优先优化方向：

- 相邻 pair 并行提取证据；
- 候选由低复杂度逐级触发；
- 只在失败 pair 上运行 APAP/flow/learned；
- RGB strip 流式、最多保留相邻 source；
- feature/flow 使用共享下采样 pyramid，但最终审计回到原始分辨率；
- 不以降低视觉门限换速度。

---

## 16. 完成定义

v5 只有同时满足以下条件才算完成：

### 架构

- 正式 RGB 路径只有 unified central strip；
- 所有内容共享 ScanPath、target coordinate 和 owner map；
- 物体世界层、2.5D object mesh、corridor RGB 和 post-overlay 均不写正式全景；
- 深度不决定物体 RGB 位置；
- 背景 seam/photometric/MultiBand 内核保持并通过黄金回归；
- raw/optimized pose 和全部候选 provenance 完整。

### 当前强回归样本

- 黑色充电器无双轮廓；
- 中部纸箱、线缆和小物件位置正确；
- 风扇右侧物件无硬矩形边和错位；
- 右侧纸箱、插头和薄结构完整；
- 物体与货架接触关系、左右顺序和遮挡正确；
- 背景墙、横梁和立柱不出现新 seam；
- 无明显重影、剪纸边、背景泄漏或彩色竖带；
- `ghost_audit`、`position_audit`、`seam_visual_audit` 和 `background_regression_audit` 全部通过。

### 泛化

- 默认配置无当前 frame、panel、深度、货架或 reference 硬编码；
- 至少完成多场景开发集和独立物理会话盲测；
- 复杂候选均可关闭、可拒绝、可消融；
- 关闭 detector 不改变目标坐标；
- reference 图不参与生成；
- 所有正式 RGB 都能追溯到真实 source UV；
- 明显重影或错误位置不得以 C 级掩盖。

---

## 17. 推荐实施顺序

不要先加入更多网络或更复杂 mesh。优先级固定为：

```text
R0 锁定真实代码与背景黄金基线
→ R1 删除双重物体坐标/二次合成，跑通全内容真实中央条带
→ R5 的基础 ghost/position 对比
→ R2 pose 优化
→ R3 局部对齐候选
→ R4 统一 seam/blend
→ R5 完整硬门
→ R6 跨场景盲测
```

R1 是对当前问题最关键的因果验证：如果去掉物体世界层和 corridor 后，重影与错位显著下降且背景不退化，就证明 v5 的单一坐标方向正确。之后只针对仍存在的相邻条带残差逐级启用 pose、APAP/flow、单应或 learned candidate，避免再次形成不可解释的多层合成系统。
