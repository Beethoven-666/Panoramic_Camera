# 修改方案 v5.1：统一中央条带全内容拼接、完整性审计与实施路线

> 版本：v5.1
> 日期：2026-07-28
> 状态：最新可实施方案，替代两份 v5 草案
> 当前强回归会话：`data/captures/run_20260727_110952_326`
> 当前问题输出：`D:\central_strip_Panoramic_Camera\new_outputs\run_20260727_110952_326_v4_r1c6`
> 实施基线候选：commit `cd53c74f753f562aeda931d9e23b7f5556994994`
> 目标：全部内容共享真正的中央条带坐标和像素生成路径，结构性消除物体重影与位置偏移，同时保留现有背景接缝质量

---

## 0. 最终裁决

v5.1 正式放弃以下物体 RGB 渲染路线：

```text
深度世界层
物体世界 footprint
DIRECT_RGBD_WORLD_OWNER
LOCAL_25D_SINGLE_RGB_OWNER
object-rich corridor 作为独立 RGB 层
identity inverse mesh 作为物体放置路径
reference-plane object replacement
post-background object overlay
```

正式 RGB 全景只允许一条路径：

```text
严格同步 RGB-D、标定与曝光
→ raw/optimized PoseRegistry
→ 全序列唯一 ScanPath
→ 全部真实 RGB 帧的统一中央条带 inverse remap
→ 相邻真实条带的受验证局部对齐候选
→ 一张全内容单调 owner/contributor 图
→ 现有 GraphCut / 光度 / 局部 MultiBand 内核
→ 完整性保护裁切
→ 静态不变量、重影、位置、接缝和背景回归审计
→ 原子发布
```

不再区分“背景渲染器”和“物体渲染器”。背景、货架、纸箱、风扇、充电器、线缆、透明或反光内容全部：

- 使用同一个扫描坐标；
- 使用同一组真实 source strips；
- 使用同一张 target valid mask；
- 使用同一张 owner/contributor 图；
- 使用同一套接缝和光度纪律；
- 使用同一套 RGB provenance。

实例、深度、OCR、track 和薄结构信息只可用于：

- source 候选质量；
- seam cost/forbidden risk；
- 遮挡和错误对应拒绝；
- `must_preserve_mask`；
- 裁切保护；
- 只读完整性、重影和位置审计。

它们不得决定独立目标坐标、物体局部尺度、世界 footprint、第二张 owner 图或第二次 RGB 合成。

---

## 1. 已核实事实与四层根因

### 1.1 当前交付存在多种几何产品

`v4_r1c6` 的只读核查结果：

| 项目 | 实测 |
|---|---|
| `report.schema` | `gemini305-dual-mosaic-report/v11` |
| metric mosaic | `trajectory_constrained_depth_aware_metric_2_5d_side_scan` |
| metric canvas | `orthographic_side_scan`，3953×1995，0.5 px/mm |
| inspection panorama | 2244×730 |
| inspection backend | `overlapping_virtual_perspective_panels_rgbd` |
| fixed pushbroom | `fixed_strip_pushbroom=false` |
| real poses | 101 |
| selected virtual panels | 12 |
| reference depth | 976.2999877929688 mm |
| reference-plane fallback RGB owners | 1,631,269 px |
| reliable-depth owners | 6,851 px |
| required object dispositions | 18 × corridor，1 × direct RGB-D owner |
| pre-seam hard-owner intervals | 11 |
| post-background hard owners | 10 |
| accepted identity inverse mesh | 1 |
| hierarchy duplicate tracks | 6 |

需要准确区分：

- metric mosaic 是独立交付产品，元数据声称不向 inspection RGB 回传；
- 最终 inspection RGB 内部仍同时存在 reference-panel、hard interval、same-panel transfer、direct depth mesh 和 post-background hard owner 等多种位置/owner 语义；
- `hierarchy_duplicate_count=6` 是 track/hierarchy 级重复证据，不单独等同于 6 个重复 RGB 物体，但说明实例去重和处置关系复杂；
- 最终视觉中的双轮廓、剪纸边和错位必须由最终 owner/provenance 与 RGB 证据确认。

### 1.2 根因一：双像素生成路径

只要同一场景内容可以经背景 reference/panel path 和物体专用 path 分别到达最终 RGB，就需要依靠 footprint 挖除、owner 锁、写入顺序和覆盖审计避免双写。

这类系统只能“尝试避免重复”，无法从结构上证明重复不存在。任意一个 mask 缺口、track split/merge、source/target panel 解耦或 post-stage 写入，都可能留下双轮廓或背景底图残影。

v5.1 的处理是删除第二条像素路径，而不是继续增加防重复条件。

### 1.3 根因二：两套位置模型

背景和物体分别使用参考平面、虚拟 panel、深度 mesh、world centroid 或 object footprint 时，同一真实内容可能得到不同目标 x/y、尺度和边界。

深度、位姿、标定和 mask 的小误差会变成物体相对货架的整体偏移。即使每个模型单独有限、无越界，也不能保证两个模型对同一内容给出一致位置。

v5.1 只保留 ScanPath + 统一中央条带 target coordinate。实例不再拥有位置模型。

### 1.4 根因三：画布法向与真实视线不一致时的误差放大

正交或固定参考深度画布将不同深度、不同视角的内容压入同一法向/尺度。对于偏离参考平面、掠射观察、边缘深度噪声或透明/反光物体：

- 深度误差会沿画布扫描方向放大；
- 位姿残差会成为物体与背景的相对位移；
- 单帧 depth/mesh 放置会把局部系统误差扩散为整物位置误差；
- object mask 边界与真实可见边界不一致时会形成剪纸边。

该分析解释为什么不能把“深度自适应实例重定时”作为新的正式位置模型。深度可以拒绝错误对应，但不能重新决定物体目标位置。

### 1.5 根因四：接缝纪律不一致

当前背景效果较好，是因为其相邻链、GraphCut、光度和局部 MultiBand 具有统一纪律。物体专用 corridor、inverse mesh、world owner 或 post-stage hard owner 使用不同采样域、光度域和接缝规则，物体与背景交界自然容易留下：

- 彩色或亮度竖带；
- 硬矩形边；
- 未对齐内容的混合重影；
- thin structure 截断；
- 背景泄漏。

v5.1 让所有内容参加同一 ordered-strip seam 和 photometric domain。

### 1.6 合并结论

四层根因构成同一因果链：

```text
第二条像素路径
→ 第二套位置模型
→ 深度/位姿误差在非正视几何中放大
→ 物体与背景使用不同接缝纪律
→ 重影 + 错位 + 不自然接缝
```

因此正确修复不是优化第二条路径，而是：

```text
一条像素路径
+ 一个目标坐标
+ 一张 owner/contributor 图
+ 一套接缝/光度纪律
+ 独立但无渲染权的完整性审计
```

---

## 2. 设计边界

### 2.1 保留

- 严格 RGB-D 会话、标定、曝光和时间戳；
- Open3D 短基线质量证据；
- ORB-SLAM3 raw trajectory；
- `calibrated_rgb_pushbroom` 的真实 pose strip layout、一次 inverse remap 和流式贡献；
- ordered adjacent-chain GraphCut；
- source-aware 全局光度与相邻 residual correction；
- 局部 MultiBand 和 valid-mask 裁切；
- PixelProvenance、原子交付、A/B/C/F；
- TSDF GLB/Viewer 的只读隔离语义；
- VisibilityMatrix、FastSAM/DIS/OCR、object inventory 和 OcclusionGraph，改为非渲染证据；
- v4 中有用的性能、模型 provenance 和 held-out 审计。

### 2.2 删除或降为 legacy diagnostic

- `inspection_multiview` 作为正式默认 RGB renderer；
- `build_inspection_identity_runtime` 返回正式 RGB owner/interval；
- `foreground_object_anchor` 正式写 RGB；
- `inspection_identity_mesh` 正式写 RGB；
- `inspection_object_rich_corridor` 返回独立 RGB corridor；
- object footprint carve-out 后由另一渲染路径填回；
- `locked_owner_panel_index` 由实例 disposition 强制指定 RGB source；
- reference 图参与 position/owner/warp；
- metric/TSDF 产品向全景回传。

### 2.3 用户已经取消的限制

v5.1 采用以下新政策：

| 能力 | v5.1 |
|---|---|
| 前景多 contributor | 允许，但只在统一 pair corridor 内，经验证后使用；否则权重坍缩到单源 |
| pose 优化/插值 | 允许，raw 永久保留；仅区间内、有界、held-out 改善后启用 |
| LightGlue/MAGSAC/Torch | 允许作为候选后端 |
| UniStitch | 允许作为候选 correspondence/layout/warp 后端 |
| 全局或 pose 级 3×3 | 允许作为候选，不因可安装而自动成为默认 |
| local APAP/flow | 模式改为 `auto`，不是固定关闭 |
| 总耗时 <300 s | 取消；只记录资源和收敛边界 |

---

## 3. 单一渲染域静态不变量

### 3.1 正式不变量

每次正式 `run()` 必须生成 `UnifiedRenderInvariantAudit`：

```text
formal_renderer_count == 1
formal_renderer_backend == unified_calibrated_central_strip/v1
target_coordinate_map_count == 1
valid_mask_count == 1
owner_map_count == 1
post_background_overlay_pixel_count == 0
world_object_sampling_pixel_count == 0
deprecated_renderer_call_count == 0
out_of_bounds_source_pixel_count == 0
border_replicated_pixel_count == 0
synthetic_or_fake_coverage_pixel_count == 0
unowned_valid_pixel_count == 0
owned_invalid_pixel_count == 0
```

混合语义：

```text
审计 blend band 外 contributor_count == 1
blend band 内 contributor_count ∈ {1, 2}
weights >= 0
sum(weights) == 1
blend band 只属于一个相邻真实 source pair
```

任何结构不变量失败直接 F，不得发布 C。

### 3.2 实例/结构自重叠

对高置信度稳定 track：

```text
structure_self_overlap_pixels == 0
duplicate_owner_run_overlap_pixels == 0
post_crop_duplicate_edge_count == 0
```

定义：

- track mask 必须经与 RGB 完全相同的最终 source→strip→crop 坐标传播；
- 不以独立 world footprint 投到目标画布；
- 只检查最终 owner/contributor 实际选择的像素；
- 一个 track 的不同 source 观测映射到相同目标区域且形成两套可见轮廓，才计自重叠/重复。

track 置信度不足、split/merge 未解决时：

- 不得伪造 `self_overlap=0`；
- 记录 `not_auditable_due_to_track_uncertainty`；
- 若该结构被列为 required，进入人工复核或 F；
- 不允许不可靠 track 反过来改变 RGB 目标坐标。

### 3.3 废弃路径调用即 F

正式编排引入 `FormalRenderTrace`：

```text
selected_renderer_backend
called_renderer_functions[]
called_legacy_pixel_paths[]
rgb_composition_stage_count
code_commit_sha
imported_package_path
effective_config_sha256
```

以下函数或等价路径在正式 RGB 生成期间被调用时直接 F：

```text
render_inspection_multiview 作为正式 renderer
overlay_foreground_object_anchors
identity mesh RGB application
object-rich corridor RGB application
metric mosaic RGB feedback
post-render patch/overlay
```

模块可以存在并由显式 legacy diagnostic 使用；“调用即 F”只针对正式 `g305-panorama` RGB 路径。

---

## 4. 目标架构

建议正式 backend/schema：

```text
renderer_backend = unified_calibrated_central_strip/v1
report schema = gemini305-panorama-report/v12
delivery schema = gemini305-panorama-delivery/v12
```

数据流：

```text
SessionContract
→ PoseRegistry(raw + candidates)
→ ScanPath
→ StructureEvidence（旁路，只读）
→ PushbroomLayout（不接收 object position/scale）
→ StripSourceContributions（全真实帧）
→ AdjacentAlignmentRegistry
→ OrderedStripSeamSolver
→ UnifiedOwnerMap
→ LocalPhotometricAndBlend
→ FullExtentValid/Owner/Provenance
→ PreservationAwareCrop
→ StripSourceAudits + StripStructureAudits
→ Ghost/Position/Seam/BackgroundRegression
→ UnifiedRenderInvariantAudit
→ PublicationAssessment
```

关键所有权：

| 数据 | 唯一生产者 | 实例证据能否改写 |
|---|---|---|
| ScanPath | pose/trajectory | 否 |
| target coordinate | pushbroom layout | 否 |
| source inverse map | calibrated strip renderer + selected local pair alignment | 只能提供风险/held-out 证据 |
| owner map | ordered-strip seam solver | 只能增加 seam risk，不得指定独立 source owner |
| RGB | real source frames | 否 |
| crop | preservation-aware valid rectangle | 可要求保留，不可生成 coverage |
| audit | source/structure auditors | 只读 |

---

## 5. PoseRegistry 与 ScanPath

### 5.1 PoseRegistry

新增统一结构：

```text
PoseRegistry
  raw_camera_to_world[]
  optimized_camera_to_world[]
  pose_kind[]
  covariance[]
  delta_translation_mm[]
  delta_rotation_deg[]
  source_factors[]
  selected_variant
```

候选因子：

- ORB-SLAM3 raw prior；
- Open3D RGB-D odometry；
- LightGlue/SuperPoint/ALIKED + MAGSAC++ E/F/PnP；
- RGB-D point-to-plane；
- 可验证的结构直线/平面；
- 有界速度和角速度平滑；
- 可验证长基线或回环。

门：

- raw 永不覆盖；
- 优化/插值不生成 RGB；
- 只在双侧真实观测区间内插值；
- 禁止首尾外推；
- delta 有物理上界；
- leave-one-factor-out、held-out RGB 和背景回归均须改善；
- 失败时整套退回 raw，不能半链混用；
- selected variant 写入每个像素 provenance。

### 5.2 ScanPath

对相机中心建立唯一、单调扫描坐标：

```text
s(t) = camera center 沿审计后主扫描曲线的弧长
x_canvas = a × s(t) + b
```

直线扫描时退化为主扫描轴投影；非匀速和轻微弯曲扫描使用弧长。

所有内容共享 `s(t)`。禁止：

- `world_footprint` 另算物体 target x；
- `Z_bg/Z_obj` 另算实例画布尺度；
- `spatial_panel_index` 另算物体位置；
- object centroid 修改 owner boundary；
- track mask 建立第二个 target transform。

### 5.3 使用全部真实帧

主扫描段全部真实 pose nodes 默认进入 strip layout，最多 160。

近重复节点可以抑制独立 owner interval，但：

- pose 和一次 RGB remap 均保留；
- 由相邻真实 strips 覆盖；
- 不生成中间 RGB；
- 抑制不依赖类别、track 或当前样本 frame id；
- 审计其最终 owner pixel count 和覆盖来源。

---

## 6. 统一中央条带与局部对齐

### 6.1 实现主干

以现有：

```text
build_calibrated_rgb_pushbroom_layout
CalibratedRGBPushbroomRenderer
render_calibrated_rgb_pushbroom
```

作为真实中央条带几何主干。

保留：

- 真实 pose owner intervals；
- 中间源中央窄条；
- 首尾向扫描外侧扩展；
- 每源一次标定 inverse remap；
- 流式相邻 source；
- valid-mask 和 owner audit；
- RGB-only 最终颜色。

不直接升格 `central_strip.py`，因为其现状仍是 reference-plane diagnostic。

### 6.2 Strip support

每个真实 source 只有：

```text
owner core
left pair overlap
right pair overlap
```

owner core 由相邻 ScanPath 位置的中点/Voronoi 分区决定。overlap 只用于：

- 对应；
- seam；
- 光度；
- 局部 inverse alignment；
- 局部 blend。

实例 bbox 不得把 source 扩展成独立 object patch。

### 6.3 对候选方案“深度自适应重定时”的吸收方式

保留其有用部分：

- 近物体的表观运动与背景不同；
- 局部 motion scale 可以帮助相邻 strip 对齐；
- depth/RGB motion 可作为模型选择证据。

不采用：

```text
实例世界 footprint → target interval
s_k = Z_bg / Z_obj → 实例正式尺度
逐实例 composite map
```

改为内容无关的局部候选：

```text
PairLocalMotionScaleCandidate
```

它只在一个真实相邻 pair corridor 内估计 inverse sampling residual：

- 由 RGB feature/flow 为主；
- depth 仅拒绝跨层对应和验证 scale 合理性；
- 不改变 ScanPath、owner interval 或 target coordinate；
- 不依赖 instance id；
- 可在多个一致 motion region 上形成一张统一、连续、边界有约束的局部 inverse field；
- held-out 不改善则 identity；
- 不允许在 object mask 边缘制造 transform jump。

### 6.4 AdjacentAlignmentRegistry

逐 pair 最低复杂度优先：

```text
A0 identity
A1 translation/similarity/affine
A2 MAGSAC++ homography 或 pose-level 3×3 candidate
A3 local APAP / regularized mesh
A4 DIS/RAFT/其它双向 flow
A5 UniStitch learned challenger
```

规则：

1. A0 通过则停止；
2. 只在当前候选未通过时评估更复杂模型；
3. train/held-out 按空间 block 和时间分组；
4. 检查 forward/backward、Jacobian、fold-over、尺度、剪切和直线；
5. 遮挡、透明、深度断层和 thin structure 边缘不能跨层拟合；
6. 最终只选择一个 composite local inverse field；
7. 未独立通过的模型不得累积叠加；
8. `local_apap_flow.mode=auto`，由证据触发；
9. learned OOD 或无提升时拒绝；
10. 所有 RGB 仍来自真实 source UV。

### 6.5 全局/pose 级 3×3

允许但不默认：

- 近平面或纯旋转区段可接受；
- object-rich 非平面场景必须同时通过近/远结构 held-out；
- 不能压平非平面内容；
- 不能绕过 ScanPath；
- 不能写入第二套 target coordinate；
- 使用后通常最高 B。

---

## 7. 一张全内容 owner 图与背景接缝内核

### 7.1 OrderedStripSeamSolver

复用现有：

```text
select_adaptive_nominal_boundaries
solve_adjacent_panel_chain
audit_panel_chain_topology
```

首版不复制算法，通过真实 strip adapter 构造：

```text
PanelLocalEvidence ← real strip valid/confidence evidence
nominal_boundaries_x ← PushbroomLayout.owner_boundaries_x
PairCorridorEvidence ← real adjacent strip pair costs
locked_owner_panel_index = None
```

名称虽含 panel，但输入语义可适配有序真实 strips。后续可重命名为 `ordered_strip_seam.py`，兼容 re-export，禁止复制出第二份 solver。

### 7.2 实例证据的合法 seam 作用

高置信度结构 evidence 可以：

- 增加切断文字、线缆、轮廓和附件的 seam cost；
- 形成 seam-forbidden risk；
- 触发更宽但仍相邻的 pair search；
- 触发局部 alignment；
- 触发 hard seam/manual review。

不得：

- 指定 `locked_owner_panel_index`；
- 从背景标签域挖出 footprint 后另行填回；
- 创建 instance owner map；
- 强制整块 source patch；
- 在 seam 之后覆盖 RGB。

### 7.3 MultiBand

统一证据驱动：

- 对齐、双向一致、非遮挡且光度一致的局部 pair 区域可 2–8 px blend；
- 遮挡边、文字、细线、透明边或 held-out 不通过时权重坍缩到单源；
- 不对未对齐高梯度边做平均；
- blend 仅属于一个相邻 pair；
- 全局多源 MultiBand 禁止；
- blend mask、source ids 和 weights 写入 provenance。

### 7.4 无安全 seam

按顺序：

1. 在同一 pair 内扩大安全搜索，但不越过资源和相邻性边界；
2. 运行更高一级 alignment candidate；
3. 选择完整覆盖的一个真实 source 形成同一 owner 图内的 hard seam；
4. 记录人工复核；
5. 无完整覆盖、产生明显重影或位置错误时 F。

不得回退到 object corridor overlay、world owner 或第二次 RGB 合成。

---

## 8. 实例完整性机制

### 8.1 StructureEvidence

实例/结构 evidence 是只读旁路：

```text
StructureEvidence
  track_id
  confidence
  required
  observation_frame_ids
  per_frame_source_masks
  thin_structure_attachment_ids
  split_merge_state
  visibility_confidence
```

来源可以是：

- FastSAM/DIS；
- 可插拔视频分割；
- OCR/appearance；
- RGB-D 连通结构；
- thin-structure detector；
- 可选 reference inventory，只用于验收。

它不能传入 position/scale/owner transform。

### 8.2 `must_preserve_mask`

保留该机制，但严格限定：

1. `per_frame_source_masks` 与 RGB 使用完全相同的 source inverse map；
2. 只在实际 owner/contributor 选中的 source 上传播；
3. 生成 full-extent target `must_preserve_mask`；
4. mask 是最终统一映射的结果，不是 world footprint；
5. mask 不生成 coverage，不扩张 RGB，不移动对象；
6. 它只参与 seam 风险、裁切和验收。

稳定 required structure：

- track 需跨足够真实帧观测；
- split/merge 已解决或有明确 parent/child；
- source masks 具有完整边界/attachment 证据；
- 不可靠检测不能成为强制 owner。

### 8.3 薄结构 attachment

为线缆、软管、插头、把手和小件记录：

```text
attachment_id
parent_track_id
source_frame_ids
endpoint_pairs
skeleton_length
connection_points
confidence
```

审计：

- attachment 端点在最终 RGB 中存在；
- 与 parent 的连接未被 seam/crop 切断；
- skeleton 长度变化可解释；
- 不因背景 mask 或深度孔洞丢失；
- pre-crop/post-crop 连通性一致。

attachment 只提高 seam/crop 保护，不获得独立 RGB overlay。

### 8.4 裁切零损失

现有：

```text
largest_valid_rectangle(valid_canvas)
```

扩展为：

```text
largest_valid_rectangle_preserving(valid_canvas, must_preserve_mask)
```

契约：

- 返回矩形内所有像素必须 valid；
- 矩形必须包含所有 high-confidence required preserve pixels；
- 不得使用黑色 RGB 值判断有效；
- 不得生成透明扩展、补洞或复制边界；
- 若不存在同时满足 valid 与 preserve 的矩形，F；
- 不可靠/非 required evidence 只产生 warning，不可误判结构 F。

必须输出：

```text
pre_crop_required_pixel_count
post_crop_required_pixel_count
cropped_required_pixel_count == 0
pre_crop_attachment_count
post_crop_attachment_count
attachment_disconnect_count == 0
```

实现建议：

- 先计算 `must_preserve_mask` 的最小包围范围；
- 使用 O(HW) constrained maximal-rectangle 算法搜索包含该范围的全 valid 矩形；
- 当前 `largest_valid_rectangle` 保留为 `must_preserve` 为空时的完全兼容路径；
- 单元测试覆盖多 valid islands、凹形 invalid 边、端点和空 mask。

---

## 9. `strip_structure_audits` 实例级只读 schema

不使用 `strip_schedule_decisions` 作为名称，避免它成为新的渲染调度入口。正式名称：

```text
g305-strip-structure-audits/v1
```

逐结构字段：

```text
track_id
required
tracking_confidence
split_merge_state
observation_frame_ids
eligible_source_frame_ids
actual_owner_run_frame_ids
actual_canvas_interval
actual_source_uv_bounds
seam_crossing_count
seam_pair_ids
blend_pixel_count
thin_structure_attachment_ids
pre_crop_pixel_count
post_crop_pixel_count
cropped_pixel_count
self_overlap_pixels
duplicate_owner_run_overlap_pixels
double_edge_p50_pixels
double_edge_p95_pixels
source_to_panorama_position_residual_p50_pixels
source_to_panorama_position_residual_p95_pixels
left_right_order_ok
occlusion_order_ok
auditability
failure_reasons
```

字段来源：

- `actual_*` 必须从最终 owner/provenance 反推；
- `position_residual` 来自多源观测到同一最终 target 的一致性；
- reference position 只能作为单独 optional audit；
- world/depth 位置只能作为诊断对比，不可充当最终 target 真值；
- track 不得因 schema 存在而改变 owner。

与全局字段合并关系：

| 全局 audit | 逐结构字段 |
|---|---|
| `strip_source_audits` | 每帧 remap、valid、owner pixel、source UV、一次采样 |
| `position_audit` | 汇总逐结构位置残差、顺序、接触关系 |
| `ghost_audit` | 汇总自重叠、双边缘、重复响应 |
| `preservation_audit` | 汇总裁切前后 required/attachment |
| `seam_visual_audit` | 汇总逐 pair/逐结构 seam 和 blend |

---

## 10. 光度

保留并统一：

```text
source-aware global linear RGB
→ adjacent residual correction
→ local safe blend
→ final monotone tone curve
```

要求：

- 背景和物体应用同一 source-domain transform；
- object track 不创建独立 gain；
- 光度样本排除过曝、欠曝、遮挡、透明、深度断层和未对齐强边；
- safe-wall 不足时可使用 held-out 通过的同表面纹理；
- pair 失败保持真实单源并降级；
- 不用 post-composition gain 掩盖竖带；
- 非有限、越界或病态 gain 仍 F。

---

## 11. 质量门

### 11.1 GhostAudit

主指标：

- 审计 blend 外 contributor count；
- 高梯度双边缘距离；
- 同一 track/source observations 的目标重叠；
- held-out feature/flow residual；
- 文字、圆孔、线缆和轮廓的重复响应；
- phase-correlation 次峰；
- 人工复核热图。

当前样本中的明显双轮廓必须为 0。生产阈值在多场景 development set 上冻结，不能按当前样本单独调。

### 11.2 PositionAudit

不依赖参考图：

- 同一结构的多帧 source→panorama P50/P95/max；
- 轮廓角点、文字角点、接触点、线缆端点；
- 相对货架层板/立柱的关系；
- 左右顺序、上下层和遮挡顺序；
- raw vs optimized pose A/B。

可选 reference audit：

- 仅在结果生成后运行；
- 不影响 pose、warp、owner、gain 或 crop；
- 单独标记 reference 时态/视角差异。

### 11.3 SeamVisualAudit

- Lab/ΔE00 P50/P95/max；
- 梯度跳变和方向差；
- owner boundary stripe energy；
- blend risk intersection；
- seam 曲率/row step；
- zero-weight wedge；
- thin structure cut count；
- structure↔background 与 background↔background 使用同一标准。

### 11.4 BackgroundRegressionAudit

定义两个层面：

1. **内核冻结**：相同 solver evidence 输入时，ordered seam、光度和 blend 内核输出逐像素/逐标量一致；
2. **结果不退化**：统一真实 strips 后，在高置信度背景 safe mask 中，竖带、断线、ΔE、梯度和曝光指标不差于冻结基线。

不能要求新真实-strip geometry 与旧 12-panel inspection panorama 全图逐像素相同；二者 target geometry 不同。

### 11.5 PreservationAudit

- high-confidence required recall = 100%；
- attachment endpoint/connection recall = 100%；
- crop loss = 0；
- 明确可观察结构缺失、错层、顺序错误或被截断时 F；
- 不可审计结构不得伪造通过。

### 11.6 UnifiedRenderInvariantAudit

§3 全部通过，是任何 A/B/C 发布的前置结构门。

---

## 12. A/B/C/F

- **A**：统一中央条带、raw 或无需恢复的可信 pose、最低复杂度对齐；结构、重影、位置、接缝、完整性和背景回归全过。
- **B**：全部视觉和结构门通过，但至少一个 pair/区段使用经 held-out 验证的 optimized/interpolated pose、APAP/flow、单应或 learned candidate。
- **C**：统一坐标、真实来源、owner topology、完整性和原子交付完整；存在经审计 hard seam、光度或一般图像质量不足，需要人工复核。不得存在明显重影、明确错误位置或 required structure 丢失。
- **F**：第二条像素路径、第二套 target coordinate、废弃路径调用、明显重影/错位、required 丢失、provenance、owner、pose、warp、valid coverage、资源硬限或原子交付任一结构失败。

纯背景尺度 fallback 如果造成可见物体拉伸或位置错误，不能仅因“没有双写”发布 C。

---

## 13. 模块级实施

### 13.1 `stitch_sequence.py`

当前实际分支：

```text
diagnostic_force=true  → render_calibrated_rgb_pushbroom
formal                 → build_inspection_identity_runtime
                         → render_inspection_multiview
```

v5.1 改为：

```text
formal                 → render_unified_calibrated_central_strip
legacy diagnostic      → render_inspection_multiview（显式命令/开关）
```

工作：

- 每次 run 首先使旧 delivery 失效；
- 建立 `FormalRenderTrace`；
- 构建 StructureEvidence，但不生成 owner；
- 调用统一 renderer 一次；
- 发布 v12 audit/schema；
- legacy 调用计数硬门；
- 保留诊断隔离和原子写入。

### 13.2 `calibrated_rgb_pushbroom.py`

保留主干，新增薄接口：

```text
StructureEvidenceProvider | None
AdjacentAlignmentRegistry | None
OrderedStripSeamAdapter
PreservationPolicy
UnifiedRenderInvariantAudit
```

`CalibratedRGBPushbroomResult` 扩展：

```text
panorama
metadata
owner_frame_id
strip_source_audits
strip_structure_audits
preservation_audit
invariant_audit
```

full-extent dense masks只在 renderer 内存/临时目录存在；正式输出只发布必要 owner/provenance 和标量审计。

删除正式消费：

- foreground anchor reservation；
- foreground owner-run handoff；
- foreground deformation；
- object-specific geometry warp。

这些可留给显式 diagnostic callback，但不得进入正式 backend。

### 13.3 `inspection_chain_seam.py`

首版复用，不复制：

- 真实 strips 构造兼容 evidence；
- `locked_owner_panel_index=None`；
- instance/structure 只进入 pair cost 或 seam-forbidden risk；
- topology audit 原样运行；
- 增加 generic 命名 alias，后续再重构文件名。

### 13.4 新建 `pose_registry.py`

- pose candidates；
- raw/optimized/interpolated provenance；
- factor audit；
- selected variant；
- raw/optimized A/B。

### 13.5 新建 `strip_alignment_registry.py`

- A0–A5 backend interface；
- train/held-out；
- FB/Jacobian/scale/line/occlusion audit；
- auto APAP/flow；
- model/version/provider/SHA；
- failure fallback。

### 13.6 新建 `structure_evidence.py`

- 可插拔 detector/tracker；
- `StructureEvidence`；
- required confidence；
- split/merge；
- thin attachment；
- source mask provider；
- 不导入 renderer 或写 RGB。

### 13.7 新建 `unified_strip_audit.py`

- `StripSourceAudit`；
- `StripStructureAudit`；
- Ghost/Position/Seam/BackgroundRegression；
- Preservation；
- UnifiedRenderInvariant；
- v12 serialization。

### 13.8 `render.py`

新增：

```text
largest_valid_rectangle_preserving
```

保留原函数作为空 preserve mask 的兼容路径。MultiBand/valid semantics 不变。

### 13.9 legacy modules

以下降为显式历史/消融：

```text
inspection_multiview.py
inspection_identity_runtime.py
inspection_identity_mesh.py
inspection_object_rich_corridor.py
foreground_object_anchor.py
metric_mosaic.py 作为独立 metric 产品
```

不急于物理删除，以便回归对照；正式 call graph 中不得出现。

---

## 14. 配置迁移

建议：

```yaml
stitch:
  formal_renderer:
    backend: unified_calibrated_central_strip

  unified_central_strip:
    use_all_real_pose_nodes: true
    structure_evidence:
      enabled: true
      affects_target_coordinates: false
      affects_owner_assignment_directly: false
      seam_risk_only: true
      crop_preservation: true
    alignment_registry:
      mode: auto
      backends:
        - identity
        - affine
        - homography
        - local_apap_flow
        - dense_flow
        - unistitch
      minimum_complexity_first: true
      require_held_out_improvement: true
    preservation:
      required_structures: high_confidence_only
      thin_structure_attachments: true
      crop_loss_allowed_pixels: 0
    background_regression:
      enabled: true

  legacy_renderers:
    inspection_multiview_formal_enabled: false
    metric_rgb_feedback_enabled: false
    object_world_layer_enabled: false
```

迁移：

- `handoff_fallback_policy.local_apap_flow_enabled` 废弃；
- 新配置为 `alignment_registry.mode=auto`；
- 旧 boolean 仅用于读取历史配置并输出迁移错误，不静默解释；
- 未安装 optional backend 时从 registry 删除并报告，不影响 identity/affine 主链；
- 不提供“恢复 world object layer”的正式开关。

数值阈值：

- 复用已验证的现有安全上限；
- 新 ghost/position/attachment 阈值先在 development set 标定；
- 盲测前冻结；
- 不在 v5.1 文档中把当前样本的 2 px、2%、ΔE00=5 等直接声明为通用正式常量。

---

## 15. 实施批次

### R0：代码与运行环境锁定

当前环境存在：

```text
D:\central_strip_Panoramic_Camera main
当前工作树 cd53c74...
主环境 import 解析到 worktrees\548d
```

必须先：

- 选择唯一实施 worktree/branch；
- `Get-Command g305-panorama` 与 `panorama_demo.__file__` 指向同一基线；
- report 写 commit SHA、import path 和 config SHA；
- 保存 `v4_r1c6`、legacy pushbroom 和关键背景 crop；
- 建立 current formal/diagnostic call graph 测试。

出口：CLI、import、HEAD、配置完全一致。

### R1：单一路径最小闭环

- formal 分支改调用现有 calibrated pushbroom；
- 禁止 identity owner、inspection object path、post overlay；
- 全部真实 pose frames 统一中央条带；
- 建立 FormalRenderTrace 和静态不变量；
- 暂不引入新 detector、pose 优化或 learned backend。

出口：

- `formal_renderer_count=1`；
- 废弃路径调用 0；
- 当前样本矩形物体覆盖和双路径残影显著消失；
- provenance/owner/valid/原子发布完整。

R1 若不能减少重影，不允许直接加入更复杂模型，必须先检查是否仍有第二条路径或旧环境调用。

### R2：ordered seam 与背景质量收口

- real-strip adapter 接入现有 ordered-chain solver；
- `locked_owner_panel_index=None`；
- 复用光度和局部 blend；
- 建立内核冻结与背景结果回归；
- 当前样本背景不得退化。

出口：背景 seam、亮度和结构线达到或优于冻结基线。

### R3：完整性旁路

- StructureEvidence；
- high-confidence required；
- thin attachment；
- `must_preserve_mask` 同映射传播；
- constrained crop；
- `strip_structure_audits`。

出口：

- detector 开/关不改变 target coordinate；
- required crop loss=0；
- attachment disconnect=0；
- 不产生 owner lock 或 overlay。

### R4：局部对齐 registry

- A0 identity；
- A1 affine；
- A2 homography；
- A3 APAP；
- A4 flow；
- A5 UniStitch；
- auto、held-out、FB/Jacobian/line/occlusion；
- PairLocalMotionScaleCandidate 作为局部候选，不是实例正式尺度。

出口：只在失败 pair 启动复杂候选；所有接受候选改善 held-out 且不退化背景。

### R5：PoseRegistry

- raw/optimized/interpolated；
- LightGlue/MAGSAC/Open3D/结构因子；
- 有界优化；
- raw/optimized A/B；
- 失败整套退回 raw。

出口：position residual 改善，背景回归不退化，provenance 完整。

### R6：审计、schema 与发布

- Ghost/Position/Seam/BackgroundRegression；
- Preservation/Invariant；
- v12 report/delivery；
- A/B/C/F；
- legacy diagnostics；
- AGENTS/README/CLI/config 同步。

出口：直接 `run()` 与 CLI 具有相同失败清理和原子语义。

### R7：跨场景冻结默认

- 多场景 development/validation/blind；
- 参数冻结；
- optional backend 消融；
- CPU/GPU/分块确定性；
- 性能和资源；
- 默认切换。

出口：盲测通过，无样本硬编码。

---

## 16. 测试与泛化

### 16.1 单元测试

- formal call graph 只有一个 renderer；
- deprecated call → F；
- contributor/weight/owner/valid 不变量；
- ScanPath 单调和弧长；
- 全真实 pose nodes；
- strip adapter；
- locked owner 恒为空；
- alignment registry 最低复杂度；
- held-out 隔离；
- APAP auto；
- structure evidence 不改变坐标；
- source mask 同映射传播；
- constrained crop；
- attachment 连通；
- structure audit 只读；
- v12 schema；
- failure/delivery 原子性。

### 16.2 合成真值

生成已知：

- 相机轨迹；
- 不同深度、尺寸和纹理；
- 近/远物体；
- 线缆/软管/插头；
- 遮挡、透明和深度孔洞；
- 位姿噪声；
- 非匀速/轻微弯曲扫描；
- 偶发动态物体。

真值只用于测试：

- 质心和轮廓误差；
- 自重叠；
- 双边缘；
- attachment；
- crop loss；
- source UV；
- owner topology。

测试真值不得在正式 renderer 中成为位置输入。

### 16.3 Metamorphic

- chunk size 不改变结果；
- 插入近重复帧不产生重复物体；
- 删除一个非关键帧只局部变化；
- detector on/off 不改变 target coordinate；
- depth risk off 不启用 world positioning；
- reference on/off 不改变 RGB；
- optional backend 缺失安全退回；
- raw/optimized 可解释；
- CPU/GPU owner topology 和等级一致；
- 正反输入存储顺序不改变捕获时间顺序。

### 16.4 跨场景矩阵

- 货架、白墙、温室、实验台、车间、杂乱室内；
- 0.3–5 m；
- 大纸箱、小电子件、圆形和重复纹理；
- 黑色、透明、反光；
- thin structures；
- 低/高纹理；
- 曝光渐变和色温变化；
- 慢/快/变速/暂停；
- 不同分辨率和帧密度；
- 新类别和无 detector 后端。

按物理 `scene_id + layout_id` 分 development/validation/blind，同场景不同 run 不跨集合。

### 16.5 必做消融

```text
legacy v11 vs unified v12
all real strips vs selected virtual panels
raw vs optimized pose
identity vs affine
APAP off vs auto
flow off vs auto
homography off vs auto
UniStitch off vs auto
structure evidence off vs on
preservation crop off vs on
hard seam vs safe local blend
```

看每场景和最差切片，不只看均值。

---

## 17. 风险与回退

| 风险 | v5.1 回退 |
|---|---|
| detector/tracker 不可用 | RGB 仍走统一 strips；关闭结构增强审计，基于可观察性降级/人工复核，不改变位置 |
| track split/merge | 不生成 owner lock；只降低 auditability |
| depth 缺失/透明 | 不改变 target coordinate；用 RGB alignment 或 identity/hard seam |
| 极近/大物体跨多个 strips | 正常由多个相邻真实 strips 构成；局部对齐/统一 seam，不创建 object patch |
| pair 无安全 alignment | 选择完整真实 source 的同一 owner 图 hard seam；明显错误则 F |
| pose 优化漂移 | 整套退回 raw |
| optional backend OOD | 拒绝该候选 |
| preserve pixels 无有效矩形可容纳 | F，不补洞、不透明扩画布 |
| 背景回归退化 | 拒绝新候选或回到已验证 seam/pose variant |
| import 指向旧 worktree | R0 失败，禁止运行正式交付 |

任何回退都不得恢复第二条 RGB 路径。

---

## 18. 性能与资源

取消 300 秒门，但记录：

- 总/阶段耗时；
- source/frame/pair/candidate 数量；
- CPU RSS；
- GPU VRAM；
- H2D/D2H；
- provider 与实际执行设备；
- 模型 path/version/SHA；
- warm-up；
- solver 迭代/收敛；
- 流式 resident strips；
- crop/audit 开销；
- CPU fallback。

优化顺序：

- A0→A5 逐级触发；
- 只对失败 pair 运行复杂候选；
- 相邻 pair 证据并行；
- full-resolution 审计、低分辨率只作候选；
- RGB strips 流式；
- 不以放宽 ghost/position/seam 门换速度。

---

## 19. 文档与治理同步

实施完成后同步：

- `AGENTS.md` 正式架构；
- `README.md`；
- `configs/demo.yaml`；
- CLI 帮助；
- report/delivery v12；
- 模块职责；
- optional dependencies；
- 测试导航；
- legacy diagnostic 声明。

明确写入：

- 正式 RGB 只有统一中央条带；
- world/identity/corridor paths 只可 legacy diagnostic；
- local APAP/flow 为 auto；
- pose/LightGlue/MAGSAC/UniStitch/3×3 候选已获准；
- reference/TSDF 不生成全景 RGB；
- 实例 evidence 无坐标和 owner 权。

---

## 20. 完成定义

### 20.1 架构

- 正式 renderer 只有一个；
- 目标坐标、valid mask、owner map 各一个；
- 所有内容统一真实中央条带；
- 废弃 pixel paths 调用为 0；
- 无 post overlay；
- 深度不决定物体目标位置；
- track/must-preserve/attachment 不决定 owner；
- 所有有效 RGB 可追溯。

### 20.2 当前强回归

- 黑色充电器无双轮廓；
- 中部纸箱、线缆和小物件位置正确；
- 风扇及相邻设备无硬矩形边；
- 右侧物体和 thin structures 完整；
- 物体与货架的接触、左右、上下和遮挡关系正确；
- 背景墙、横梁、立柱无新竖带或断线；
- crop loss=0；
- Ghost/Position/Seam/BackgroundRegression/Preservation/Invariant 全过。

### 20.3 泛化

- 无固定 frame/panel/depth/货架/reference 硬编码；
- detector 开关不改变坐标；
- optional backend 可关闭、可拒绝、可消融；
- 多场景盲测通过；
- 明显重影、错误位置或 required 丢失不能以 C 掩盖；
- 无 300 秒门，但资源记录完整。

---

## 21. 推荐执行顺序

```text
R0 锁定实际代码与环境
→ R1 切断第二路径，跑通全内容真实中央条带
→ R2 接入现有 ordered seam，恢复/锁定背景质量
→ R3 增加 must_preserve、attachment 和只读结构审计
→ R4 逐 pair auto alignment
→ R5 有界 pose 优化
→ R6 v12 硬门与发布
→ R7 跨场景盲测
```

最重要的因果验证是 R1：

> 在不加入新 detector、mesh、flow、单应或 learned model 的条件下，仅删除第二条 RGB 路径并恢复全部真实中央条带，重影和物体位置偏移是否显著下降。

如果 R1 不改善，优先检查：

1. 实际运行的 import/CLI 是否仍指向旧 worktree；
2. 是否仍有 legacy overlay/owner interval；
3. 是否错误使用 12-panel inspection 输出；
4. owner/provenance 是否仍有双 source；
5. ScanPath/scale/pose 是否本身错误。

不得在根因未闭环时用更复杂模型掩盖结构问题。
