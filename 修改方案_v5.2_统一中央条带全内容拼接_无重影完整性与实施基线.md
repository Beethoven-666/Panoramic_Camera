# 修改方案 v5.2：统一中央条带全内容拼接、无重影完整性与实施基线

> 版本：v5.2
> 日期：2026-07-28
> 状态：最新可实施方案，替代 v5、v5.1 草案
> 当前强回归会话：`data/captures/run_20260727_110952_326`
> 当前问题输出：`D:\central_strip_Panoramic_Camera\new_outputs\run_20260727_110952_326_v4_r1c6`
> 实施基线候选：commit `cd53c74f753f562aeda931d9e23b7f5556994994`
> 目标：全部内容共享真正的中央条带坐标和唯一 RGB 生成路径，结构性消除物体重影、位置偏移与独立物体接缝

---

## 0. 最终裁决

v5.2 正式放弃：

```text
深度世界层物体 RGB
物体 world footprint 决定目标位置
DIRECT_RGBD_WORLD_OWNER
LOCAL_25D_SINGLE_RGB_OWNER
object-rich corridor 作为独立 RGB 层
identity inverse mesh 作为物体放置路径
reference-plane object replacement
post-background object overlay
```

正式 RGB 全景只有一条路径：

```text
严格同步 RGB-D、标定与曝光
→ raw/optimized PoseRegistry
→ 全序列唯一 ScanPath
→ 全部真实 RGB 帧的统一中央条带 inverse remap
→ 相邻真实条带的受验证局部对齐候选
→ 一张全内容单调 owner/contributor 图
→ 现有 GraphCut / 光度 / 局部 MultiBand 内核
→ must-preserve 约束的全 valid 矩形裁切
→ 静态不变量、重影、位置、接缝、完整性和背景回归审计
→ 原子发布
```

背景、货架、纸箱、风扇、充电器、线缆、透明或反光内容全部：

- 使用同一个扫描坐标；
- 使用同一组真实 source strips；
- 使用同一张 target valid mask；
- 使用同一张 owner/contributor 图；
- 使用同一套接缝和光度纪律；
- 使用同一套 RGB provenance。

实例、深度、OCR、track 和薄结构证据只可用于：

- source 质量和可观察性；
- seam cost/forbidden risk；
- 遮挡和错误对应拒绝；
- `must_preserve_mask`；
- 裁切保护；
- 只读完整性、重影和位置审计。

它们不得决定：

- 物体目标坐标；
- 物体专用水平尺度；
- object world footprint；
- 第二张 owner 图；
- 强制 RGB source owner；
- 第二次 RGB 合成。

---

## 1. v5.2 合并裁决

### 1.1 吸收项

v5.2 在 v5.1 实施版基础上吸收：

1. 两份草案的分歧裁决表；
2. 真狭缝位置原理的完整说明；
3. development→冻结→blind hold-out 的阈值流程；
4. PixelProvenance `sampling_model` 封闭白名单；
5. R0 黄金基线的具体文件清单；
6. 每个问题区域的标准验收可视化产品。

### 1.2 明确不吸收

| 候选设计 | v5.2 裁决 | 原因 |
|---|---|---|
| `s = Z_bg/Z_obj` 作为实例正式尺度 | 不采用 | 重新让深度和实例 mask 决定物体几何 |
| 逐实例 composite transform | 不采用 | 会在实例边界产生第二套 sampling rule |
| `strip_schedule_decisions` 作为调度入口 | 不采用 | 容易演变成新的 owner/位置控制器 |
| `must_preserve_mask` 生成目标 footprint | 不采用 | preserve 必须通过相同 RGB 映射传播 |
| 裁切失败时保留透明边或补洞 | 不采用 | 正式输出必须是全 valid RGB 矩形 |
| 实例核 seam cost 无穷大 | 不采用 | 可能等价于不可审计 owner lock |
| local APAP/flow 默认关闭 | 不采用 | 用户已取消该既定限制，v5.2 使用 `auto` |
| metric/TSDF 产品全部删除 | 不默认采用 | 可保留为完全隔离的只读产品，不影响 RGB |

### 1.3 关键分歧的最终选择

| 分歧 | v5.2 | 理由 |
|---|---|---|
| 物体位置 | 全真实帧 + 真中央窄条 + ScanPath | detector/depth 开关不改变坐标 |
| 稀疏帧近物体残差 | 内容无关的 pair-local motion-scale/alignment candidate | 不建立实例专用坐标 |
| 背景算法不变 | 冻结 seam/photometric/blend 内核；允许 real-strip adapter | 新旧 target geometry 不同，不能要求全图像素相同 |
| 实例保护 | bounded seam risk + must-preserve crop audit | 不产生 owner lock |
| 多 contributor | 证据驱动；失败坍缩到单源 | 用户已取消前景 owner-only |
| 阈值 | 多场景开发集确定，冻结后盲测 | 禁止当前样本手工常量 |
| 实例 schema | `strip_structure_audits` 只读 | 不允许审计结构反向控制渲染 |
| 实施顺序 | 先单路径和背景质量，再完整性、alignment、pose | 先验证根因，减少变量 |

---

## 2. 已核实事实与四层根因

### 2.1 当前交付事实

`v4_r1c6`：

| 项目 | 实测 |
|---|---|
| `report.schema` | `gemini305-dual-mosaic-report/v11` |
| metric mosaic | `trajectory_constrained_depth_aware_metric_2_5d_side_scan` |
| metric canvas | `orthographic_side_scan`，3953×1995，0.5 px/mm |
| inspection panorama | 2244×730 |
| inspection backend | `overlapping_virtual_perspective_panels_rgbd` |
| fixed pushbroom | `fixed_strip_pushbroom=false` |
| real poses | 101 |
| selected virtual panels | 12 |
| reference depth | 976.2999877929688 mm |
| reference-plane fallback owners | 1,631,269 px |
| reliable-depth owners | 6,851 px |
| required dispositions | 18 × corridor，1 × direct RGB-D owner |
| pre-seam hard-owner intervals | 11 |
| post-background hard owners | 10 |
| accepted identity inverse mesh | 1 |
| hierarchy duplicate tracks | 6 |

准确解释：

- metric mosaic 是独立产品，不应被直接宣称为最终 RGB 重影来源；
- 最终 inspection RGB 内部仍存在 reference-panel、hard interval、same-panel transfer、direct depth mesh 和 post-background hard owner 等多种位置/owner 语义；
- `hierarchy_duplicate_count=6` 是 track 级重复证据，不单独等同于 6 个重复 RGB 物体；
- 最终重影必须由 owner/provenance 和实际 RGB 双边缘审计确认。

### 2.2 根因一：双像素路径

同一内容可以经背景 reference/panel path 和物体专用 path 分别到达最终 RGB时，只能依靠 footprint 挖除、owner 锁、写入顺序和覆盖检查避免双写。

只要第二条路径存在，重影只能被“尽量避免”，不能被结构性证明为不存在。

### 2.3 根因二：两套位置模型

背景和物体分别使用参考平面、虚拟 panel、depth mesh、world centroid 或 object footprint 时，同一内容可能得到不同的目标坐标、尺度和边界。

即使两个模型各自有限、无越界，也不能保证它们对同一物体和货架给出一致相对位置。

### 2.4 根因三：画布法向与视线不一致的误差放大

正交或固定参考深度画布会把不同深度和视角压入一个法向/尺度。近物体、掠射观察、边缘深度噪声或透明/反光内容会出现：

- 深度误差沿扫描方向放大；
- 位姿残差变成物体与背景相对位移；
- 单帧 depth/mesh 系统误差扩散为整物偏移；
- mask 边界变成剪纸边。

这也是 v5.2 不允许实例深度重定时成为正式位置模型的原因。

### 2.5 根因四：接缝纪律不一致

背景使用统一的相邻链、GraphCut、光度和 MultiBand。物体 corridor、inverse mesh、world owner 或 post-stage hard owner 使用不同采样域、光度域和接缝规则，因此容易产生：

- 亮度/颜色竖带；
- 硬矩形边；
- 未对齐平均重影；
- thin structure 截断；
- 背景泄漏。

### 2.6 因果链

```text
第二条像素路径
→ 第二套位置模型
→ 非正视几何放大深度/位姿误差
→ 物体与背景使用不同接缝纪律
→ 重影 + 错位 + 不自然接缝
```

v5.2 的解法：

```text
一条像素路径
+ 一个目标坐标
+ 一张 owner/contributor 图
+ 一套接缝/光度纪律
+ 无渲染权的完整性审计
```

---

## 3. 真狭缝位置原理与 ScanPath

### 3.1 原理

连续水平侧扫中，一个静态场景点在相机中央视线经过它时被中央狭缝记录。理想狭缝极限下：

```text
场景点的 panorama x
= 中央穿越时刻的相机扫描位置
```

这一定义不要求把点先投到背景平面或物体 mesh。

对不同深度的静态内容：

- 远背景和近物体的中央穿越时刻不同；
- 但每个点的目标 x 都由相机经过该点扫描位置的时刻决定；
- 不需要 `Z_bg/Z_obj` 重算物体正式尺度；
- 不需要 detector 判断它属于背景还是物体；
- 不需要 world footprint 搬动物体。

工程实现不是无限窄连续狭缝，而是有限帧率、有限条带。其误差来源：

- 相邻 pose 间距；
- 中央条带宽度；
- 非匀速和旋转；
- rolling/timestamp；
- 标定和 pose 残差；
- 局部遮挡。

这些残差由全真实帧、窄 owner core、相邻 overlap、PoseRegistry 和 pair-local alignment 处理，不引入实例坐标。

### 3.2 ScanPath

```text
s(t) = camera center 沿审计后扫描主曲线的弧长
x_canvas = a × s(t) + b
```

直线扫描退化为主轴投影；非匀速和轻微弯曲使用弧长。

所有内容共享 `s(t)`。禁止：

- world footprint 另算物体 x；
- `Z_bg/Z_obj` 另算实例尺度；
- object centroid 修改 boundary；
- track mask 建立 target transform；
- detector 开关改变 target coordinate。

### 3.3 使用全部真实帧

主扫描段全部真实 pose nodes 默认进入 layout，最多 160。

近重复节点可以抑制独立 owner interval，但：

- pose 和一次 RGB remap 保留；
- 由相邻真实 strips 覆盖；
- 不生成中间 RGB；
- 不依赖类别、track 或固定 frame id；
- 审计最终 owner pixel count 和覆盖来源。

---

## 4. PoseRegistry

```text
PoseRegistry
  raw_camera_to_world[]
  optimized_camera_to_world[]
  pose_kind[]
  covariance[]
  delta_translation_mm[]
  delta_rotation_deg[]
  source_factors[]
  selected_variant
```

允许因子：

- ORB-SLAM3 raw prior；
- Open3D RGB-D odometry；
- LightGlue/SuperPoint/ALIKED + MAGSAC++ E/F/PnP；
- RGB-D point-to-plane；
- 结构直线/平面；
- 有界速度和角速度平滑；
- 可验证长基线和回环。

门：

- raw 永不覆盖；
- 优化/插值不生成 RGB；
- 只在双侧真实观测区间内插值；
- 禁止首尾外推；
- delta 有物理上界；
- leave-one-factor-out、held-out RGB 和背景回归均须改善；
- 失败整套退回 raw，不允许半链混用；
- selected variant 写入像素 provenance。

---

## 5. 单一渲染域静态不变量

### 5.1 UnifiedRenderInvariantAudit

```text
formal_renderer_count == 1
formal_renderer_backend == unified_calibrated_central_strip/v1
target_coordinate_map_count == 1
valid_mask_count == 1
owner_map_count == 1
post_background_overlay_pixel_count == 0
world_object_sampling_pixel_count == 0
deprecated_renderer_call_count == 0
out_of_bounds_source_pixel_count == 0
border_replicated_pixel_count == 0
synthetic_or_fake_coverage_pixel_count == 0
unowned_valid_pixel_count == 0
owned_invalid_pixel_count == 0
```

混合：

```text
审计 blend band 外 contributor_count == 1
blend band 内 contributor_count ∈ {1, 2}
weights >= 0
sum(weights) == 1
blend band 只属于一个相邻真实 source pair
```

任何结构不变量失败直接 F。

### 5.2 `sampling_model` 白名单

每个最终有效像素：

```text
sampling_model ∈ {
  central_strip,
  central_strip_aligned,
  audited_safe_blend_band
}
```

解释：

- `central_strip`：统一标定中央条带直接单源采样；
- `central_strip_aligned`：经已接受 pair-local inverse alignment 后单源采样；
- `audited_safe_blend_band`：同一相邻 pair 的安全局部混合。

其它值、缺失值或 legacy world/object sampling model 直接 F。

### 5.3 结构自重叠

对高置信度稳定 track：

```text
structure_self_overlap_pixels == 0
duplicate_owner_run_overlap_pixels == 0
post_crop_duplicate_edge_count == 0
```

track mask 必须经与 RGB 相同的 source→strip→crop 映射传播；不得用独立 world footprint。

track 不可靠时记录 `not_auditable_due_to_track_uncertainty`，不得伪造 0，也不得反向改变 RGB。

### 5.4 FormalRenderTrace

记录：

```text
selected_renderer_backend
called_renderer_functions[]
called_legacy_pixel_paths[]
rgb_composition_stage_count
code_commit_sha
imported_package_path
effective_config_sha256
```

正式 RGB 期间调用以下等价路径直接 F：

```text
render_inspection_multiview 作为正式 renderer
foreground object anchor RGB application
identity mesh RGB application
object-rich corridor RGB application
metric/TSDF RGB feedback
post-render patch/overlay
```

legacy diagnostic 可以显式调用，但不得发布正式 panorama/delivery。

---

## 6. 统一中央条带与局部对齐

### 6.1 实现主干

使用现有：

```text
build_calibrated_rgb_pushbroom_layout
CalibratedRGBPushbroomRenderer
render_calibrated_rgb_pushbroom
```

保留：

- 真实 pose owner intervals；
- 中间源中央窄条；
- 首尾向扫描外侧扩展；
- 每源一次标定 inverse remap；
- 流式相邻 source；
- valid-mask、owner 和 RGB provenance。

不直接升格 reference-plane `central_strip.py`。

### 6.2 Strip support

每个 source：

```text
owner core
left pair overlap
right pair overlap
```

owner core 由相邻 ScanPath 位置的中点/Voronoi 分区决定。overlap 只用于对应、seam、光度、局部 inverse alignment 和局部 blend。

实例 bbox 不得扩展成 object patch。

### 6.3 AdjacentAlignmentRegistry

最低复杂度优先：

```text
A0 identity
A1 translation/similarity/affine
A2 MAGSAC++ homography 或 pose-level 3×3
A3 local APAP / regularized mesh
A4 DIS/RAFT/其它双向 flow
A5 UniStitch learned challenger
```

规则：

1. A0 通过即停止；
2. 只在当前候选失败时评估更复杂模型；
3. train/held-out 按空间 block 和时间分组；
4. 检查 FB、Jacobian、fold-over、尺度、剪切、直线和遮挡；
5. 深度只拒绝跨层对应和验证模型；
6. 最终选择一个 composite local inverse field；
7. 未独立通过的模型不得叠加；
8. `local_apap_flow.mode=auto`；
9. learned OOD/无提升拒绝；
10. 所有 RGB 来自真实 source UV。

### 6.4 内容无关的局部运动尺度

吸收“近物体表观运动不同”的有效观察，但不建立实例模型。

允许：

```text
PairLocalMotionScaleCandidate
```

仅在真实相邻 pair corridor 内：

- RGB feature/flow 为主；
- depth 只拒绝跨层对应；
- 不接收 instance id；
- 不修改 ScanPath、owner interval 或 target coordinate；
- 形成连续、有边界约束的局部 inverse field；
- 不在 mask 边缘产生 transform jump；
- held-out 不改善则 identity；
- 作为 A1/A3 的候选，由 `auto` 触发，不固定关闭。

禁止：

```text
s = Z_bg/Z_obj 作为实例正式尺度
instance world footprint → target interval
逐实例 composite transform
```

### 6.5 单一 RGB remap

每个 source 的最终 sampling coordinates：

```text
统一标定 inverse remap
∘ 选中的 pair-local inverse alignment
```

只执行一次 RGB sampling。不得生成背景后重新采样物体。

---

## 7. 一张 owner 图与背景接缝内核

### 7.1 OrderedStripSeamSolver

复用：

```text
select_adaptive_nominal_boundaries
solve_adjacent_panel_chain
audit_panel_chain_topology
```

通过 real-strip adapter：

```text
PanelLocalEvidence ← strip valid/confidence
nominal_boundaries_x ← PushbroomLayout.owner_boundaries_x
PairCorridorEvidence ← adjacent strip costs
locked_owner_panel_index = None
```

不复制第二份 solver。

### 7.2 结构 evidence 的合法作用

可以：

- 增加切断文字、线缆、轮廓和 attachment 的 bounded seam cost；
- 形成 seam-forbidden risk；
- 触发更宽但仍相邻的 pair search；
- 触发 local alignment；
- 触发 hard seam/manual review。

不得：

- 指定 locked owner；
- 从 owner 域挖出 footprint；
- 创建 instance owner map；
- 强制整块 source patch；
- seam 后覆盖 RGB。

### 7.3 MultiBand

- 对齐、双向一致、非遮挡且光度一致时可 2–8 px blend；
- 遮挡边、文字、细线、透明边或 held-out 失败时坍缩到单源；
- 不平均未对齐高梯度边；
- blend 只属于一个相邻 pair；
- 禁止全局多源 MultiBand；
- mask、source ids 和 weights 写入 provenance。

### 7.4 无安全 seam

1. 在同一 pair 内扩大安全搜索；
2. 运行更高一级 alignment；
3. 选择完整真实 source 形成同一 owner 图内的 hard seam；
4. 记录人工复核；
5. 无完整覆盖、明显重影或位置错误时 F。

不得恢复 object corridor、world owner 或 overlay。

---

## 8. 完整性、attachment 与裁切

### 8.1 StructureEvidence

```text
StructureEvidence
  track_id
  confidence
  required
  observation_frame_ids
  per_frame_source_masks
  thin_structure_attachment_ids
  split_merge_state
  visibility_confidence
```

它不能包含或输出：

- target position；
- scale transform；
- owner assignment；
- target footprint。

### 8.2 `must_preserve_mask`

1. source masks 与 RGB 使用完全相同的 inverse map；
2. 只在实际 owner/contributor source 上传播；
3. 生成 full-extent target preserve mask；
4. 不用 world footprint；
5. 不生成 coverage、扩张 RGB 或移动对象；
6. 只用于 seam 风险、裁切和验收。

### 8.3 Thin structure attachment

```text
attachment_id
parent_track_id
source_frame_ids
endpoint_pairs
skeleton_length
connection_points
confidence
```

审计：

- endpoint 存在；
- 与 parent 连接未被 seam/crop 切断；
- skeleton 变化可解释；
- pre/post crop 连通性一致。

attachment 不获得 overlay。

### 8.4 全 valid 零损失裁切

新增：

```text
largest_valid_rectangle_preserving(
  valid_canvas,
  must_preserve_mask
)
```

契约：

- 矩形内全部像素 valid；
- 包含全部 high-confidence required preserve pixels；
- 不按 RGB 黑色判断有效；
- 不生成透明边、补洞或复制边界；
- 无满足条件的矩形时 F；
- preserve 为空时与现有 `largest_valid_rectangle` 完全兼容。

输出：

```text
pre_crop_required_pixel_count
post_crop_required_pixel_count
cropped_required_pixel_count == 0
pre_crop_attachment_count
post_crop_attachment_count
attachment_disconnect_count == 0
```

---

## 9. 只读 `strip_structure_audits`

schema：

```text
g305-strip-structure-audits/v1
```

逐结构：

```text
track_id
required
tracking_confidence
split_merge_state
observation_frame_ids
eligible_source_frame_ids
actual_owner_run_frame_ids
actual_canvas_interval
actual_source_uv_bounds
seam_crossing_count
seam_pair_ids
blend_pixel_count
thin_structure_attachment_ids
pre_crop_pixel_count
post_crop_pixel_count
cropped_pixel_count
self_overlap_pixels
duplicate_owner_run_overlap_pixels
double_edge_p50_pixels
double_edge_p95_pixels
source_to_panorama_position_residual_p50_pixels
source_to_panorama_position_residual_p95_pixels
left_right_order_ok
occlusion_order_ok
auditability
failure_reasons
```

规则：

- `actual_*` 从最终 owner/provenance 反推；
- position residual 来自多源观测一致性；
- reference/world position 只能单独旁证；
- schema 不得反向控制 owner 或 coordinate。

---

## 10. 光度

```text
source-aware global linear RGB
→ adjacent residual correction
→ local safe blend
→ final monotone tone curve
```

- 背景和物体应用同一 source-domain transform；
- track 不创建 gain；
- 样本排除过曝、欠曝、遮挡、透明、深度断层和未对齐强边；
- safe-wall 不足时使用 held-out 通过的同表面纹理；
- pair 失败保持单源并降级；
- 不用 post-composition gain 掩盖竖带；
- 非有限、越界、病态 gain 为 F。

---

## 11. 质量门与阈值冻结

### 11.1 GhostAudit

- blend 外 contributor count；
- 高梯度双边缘距离；
- 同一 track/source observation 的目标重叠；
- held-out feature/flow residual；
- 文字、圆孔、线缆和轮廓重复响应；
- phase-correlation 次峰；
- 人工复核热图。

当前样本明显双轮廓必须为 0。

### 11.2 PositionAudit

- 多帧 source→panorama P50/P95/max；
- 轮廓角点、文字角点、接触点、线缆端点；
- 相对货架层板/立柱关系；
- 左右、上下层和遮挡顺序；
- raw vs optimized pose A/B。

reference 仅在生成后验收，不参与生成。

### 11.3 SeamVisualAudit

- Lab/ΔE00 P50/P95/max；
- 梯度跳变和方向；
- boundary stripe energy；
- blend risk intersection；
- seam curvature/row step；
- zero-weight wedge；
- thin-structure cut count；
- 结构↔背景和背景↔背景使用同一门。

### 11.4 PreservationAudit

- high-confidence required recall=100%；
- attachment endpoint/connection recall=100%；
- crop loss=0；
- 明确可观察结构缺失、错层、顺序错误或被截断时 F；
- 不可审计结构不得伪造通过。

### 11.5 BackgroundRegressionAudit

1. 相同 evidence 输入时，seam/photometric/blend 内核输出逐像素/逐标量一致；
2. 统一 real strips 后，背景 safe mask 中竖带、断线、ΔE、梯度和曝光不差于冻结基线。

不能要求新 real-strip geometry 与旧 12-panel 全图逐像素一致。

### 11.6 阈值冻结方法

所有新增阈值：

```text
跨场景 development set
→ 按原始分辨率和结构尺度归一化确定
→ 冻结配置、模型、provider 和阈值
→ 独立物理会话 validation
→ blind hold-out
```

规则：

- 当前样本只用于回归，不用于单独定阈值；
- 同一扫描相邻帧不得拆到 train/held-out；
- 冻结后不得因 blind 结果回调；
- 修改阈值或模型需要新版本和重新盲测；
- 不把 2 px、2%、ΔE00=5 等未经跨场景验证的值直接写成正式通用常量。

---

## 12. 标准验收可视化

当前强回归区域：

- 左中部黑色充电器与白色底座；
- 中部纸箱、线缆和下方小物件；
- 风扇及左右相邻设备；
- 右侧纸箱、插头和薄结构；
- 黄色横梁、深色立柱和白墙全部 seam。

每个区域必须输出：

```text
baseline_crop.png
v5_2_crop.png
owner_source_visualization.png
ghost_heatmap.png
position_residual_overlay.png
seam_blend_mask.png
preservation_overlay.png
```

并提供：

```text
source frame ids
source UV
selected alignment model
seam pair id
blend weights
ghost/position/seam scalar metrics
pre/post crop structure counts
```

这些是验收产品，不参与生成。

---

## 13. PixelProvenance v2 与报告

每像素：

```text
target_xy
owner_or_contributor_count
source_frame_ids[]
source_uv[]
weights[]
scan_coordinate
pose_kind
pose_id
sampling_model
alignment_model
alignment_model_id
photometric_transform_id
blend_pair_id
depth_used_for_rejection_only
```

报告：

```text
renderer_backend
formal_render_path
code_commit_sha
imported_package_path
effective_config_sha256
formal_render_trace
pose_registry
scan_path_audit
strip_source_audits
strip_structure_audits
pair_alignment_candidates
selected_alignment_models
unified_owner_topology
ghost_audit
position_audit
seam_visual_audit
preservation_audit
background_regression_audit
unified_render_invariant_audit
runtime_profile
disabled_legacy_object_render_paths
```

硬门：

- generated=0；
- unsupported=0；
- source UV 有限、有效；
- weights 非负、和为1；
- sampling model 在白名单；
- 无 world target footprint；
- 无 post overlay。

---

## 14. 模块实施

### 14.1 `stitch_sequence.py`

当前：

```text
diagnostic_force=true → calibrated pushbroom
formal                → identity runtime
                        → inspection multiview
```

v5.2：

```text
formal           → render_unified_calibrated_central_strip
legacy diagnostic→ inspection multiview
```

实现：

- 首先失效旧 delivery；
- FormalRenderTrace；
- StructureEvidence 不生成 owner；
- 调用正式 renderer 一次；
- v12 schema；
- legacy call 硬门；
- 原子交付。

### 14.2 `calibrated_rgb_pushbroom.py`

新增薄接口：

```text
StructureEvidenceProvider | None
AdjacentAlignmentRegistry | None
OrderedStripSeamAdapter
PreservationPolicy
UnifiedRenderInvariantAudit
```

result 扩展：

```text
panorama
metadata
owner_frame_id
strip_source_audits
strip_structure_audits
preservation_audit
invariant_audit
```

full-extent dense masks 只在 renderer 内存/临时目录。

正式路径删除 foreground anchor reservation、foreground owner-run handoff、foreground deformation 和 object-specific geometry warp；它们只可 explicit diagnostic。

### 14.3 `inspection_chain_seam.py`

- 复用 solver，不复制；
- real-strip evidence adapter；
- locked owner 为 None；
- structure 只进入 bounded risk；
- topology audit 原样运行。

### 14.4 新模块

| 模块 | 职责 |
|---|---|
| `pose_registry.py` | raw/optimized/interpolated pose 和 factors |
| `strip_alignment_registry.py` | A0–A5、held-out、auto |
| `structure_evidence.py` | tracker、required、attachment、source masks；不写 RGB |
| `unified_strip_audit.py` | source/structure/ghost/position/seam/preservation/invariant |

### 14.5 `render.py`

新增：

```text
largest_valid_rectangle_preserving
```

preserve 为空时与原函数兼容。

### 14.6 Legacy

```text
inspection_multiview.py
inspection_identity_runtime.py
inspection_identity_mesh.py
inspection_object_rich_corridor.py
foreground_object_anchor.py
metric_mosaic.py（独立只读产品）
```

保留回归对照；正式 RGB call graph 不得出现。

---

## 15. 配置迁移

```yaml
stitch:
  formal_renderer:
    backend: unified_calibrated_central_strip

  unified_central_strip:
    use_all_real_pose_nodes: true
    structure_evidence:
      enabled: true
      affects_target_coordinates: false
      affects_owner_assignment_directly: false
      seam_risk_only: true
      crop_preservation: true
    alignment_registry:
      mode: auto
      backends:
        - identity
        - affine
        - homography
        - local_apap_flow
        - dense_flow
        - unistitch
      minimum_complexity_first: true
      require_held_out_improvement: true
    preservation:
      required_structures: high_confidence_only
      thin_structure_attachments: true
      crop_loss_allowed_pixels: 0
    background_regression:
      enabled: true

  legacy_renderers:
    inspection_multiview_formal_enabled: false
    metric_rgb_feedback_enabled: false
    object_world_layer_enabled: false
```

迁移：

- 废弃 `handoff_fallback_policy.local_apap_flow_enabled`；
- 使用 `alignment_registry.mode=auto`；
- 旧 boolean 触发明确迁移错误，不静默解释；
- optional backend 缺失时报告并从 registry 删除；
- 不提供恢复 world object layer 的正式开关。

---

## 16. 实施批次

### R0：锁定代码、环境和黄金基线

当前存在：

```text
D:\central_strip_Panoramic_Camera main
工作树 commit cd53c74...
主环境 import → worktrees\548d
```

先统一：

- worktree/branch/commit；
- `Get-Command g305-panorama`；
- `panorama_demo.__file__`；
- config SHA；
- report code SHA。

黄金基线具体保存：

```text
v4_r1c6/panorama.jpg
full_extent inspection RGB/alpha
valid mask
owner_frame_id / owner visualization
all pair seam paths
pair corridors
blend masks
photometric gains and residual gains
background safe mask
background risk mask
关键问题区域 crops
report/delivery/inspection/mosaic metadata
runtime profile
```

另保存：

```text
legacy calibrated pushbroom output
legacy pushbroom owner/provenance
legacy background seam/photometric metrics
```

黄金文件只读，并记录 SHA256、commit 和 config SHA。

出口：CLI、import、HEAD、配置完全一致。

### R1：单一路径最小闭环

- formal 改用现有 calibrated pushbroom；
- 禁止 identity owner、inspection object path、overlay；
- 全真实 pose frames；
- FormalRenderTrace 和 sampling-model 白名单；
- 不加入新 detector、pose 优化或 learned backend。

出口：

- renderer=1；
- legacy calls=0；
- 双路径残影显著消失；
- provenance/owner/valid/原子发布完整。

若 R1 不改善，先检查旧路径/环境/owner，不得用复杂模型掩盖。

### R2：ordered seam 与背景质量

- real-strip adapter；
- locked owner=None；
- 复用光度和局部 blend；
- 内核冻结；
- 背景结果回归。

出口：背景 seam、亮度和结构线达到或优于冻结基线。

### R3：完整性旁路

- StructureEvidence；
- required confidence；
- attachment；
- preserve mask 同映射传播；
- constrained crop；
- strip structure audits。

出口：

- detector on/off 坐标不变；
- crop loss=0；
- attachment disconnect=0；
- 无 owner lock/overlay。

### R4：局部对齐

- A0–A5；
- auto APAP/flow；
- held-out/FB/Jacobian/line/occlusion；
- PairLocalMotionScaleCandidate 内容无关；
- sampling model provenance。

出口：复杂候选只在失败 pair 启动，且改善 held-out 和背景。

### R5：PoseRegistry

- raw/optimized/interpolated；
- LightGlue/MAGSAC/Open3D/结构因子；
- 有界优化；
- raw/optimized A/B；
- 失败整套退回 raw。

出口：position residual 改善，背景不退化。

### R6：审计、schema 和可视化

- Ghost/Position/Seam/Preservation/BackgroundRegression/Invariant；
- v12 report/delivery；
- A/B/C/F；
- §12 标准可视化；
- CLI/direct run 同失败清理。

### R7：阈值冻结和盲测

- development 标定；
- 参数/模型/provider 冻结；
- validation；
- blind；
- 消融和确定性；
- 默认切换。

---

## 17. 测试与泛化

### 17.1 单元

- formal call graph；
- deprecated call→F；
- sampling model 白名单；
- contributor/weights/owner/valid；
- ScanPath；
- all real poses；
- strip adapter；
- locked owner=None；
- alignment auto；
- held-out；
- structure evidence 不改变坐标；
- preserve 同映射；
- constrained crop；
- attachment；
- read-only audit；
- v12；
- 原子失败。

### 17.2 合成

已知轨迹、深度、近远物体、thin structures、遮挡、透明、位姿噪声、变速/弯曲。

真值仅用于测试：

- 中央穿越位置；
- 轮廓/质心；
- 自重叠；
- 双边缘；
- attachment；
- crop loss；
- source UV；
- topology。

不得向正式 renderer 提供真值位置。

### 17.3 Metamorphic

- chunk 不变；
- 近重复帧不重复；
- 删除非关键帧局部变化；
- detector on/off 坐标不变；
- depth off 不启用 world positioning；
- reference on/off RGB 不变；
- optional backend 缺失安全退回；
- raw/optimized 可解释；
- CPU/GPU topology/grade 一致。

### 17.4 跨场景

- 货架、白墙、温室、实验台、车间、杂乱室内；
- 0.3–5 m；
- 大小物体、重复纹理；
- 黑色、透明、反光；
- thin structures；
- 不同纹理/曝光/速度/分辨率/帧密度；
- 新类别和无 detector backend。

按物理 scene/layout 分 development/validation/blind。

### 17.5 消融

```text
legacy v11 vs unified v12
all real strips vs virtual panels
raw vs optimized pose
identity vs affine
APAP off vs auto
flow off vs auto
homography off vs auto
UniStitch off vs auto
structure evidence off vs on
preservation crop off vs on
hard seam vs safe blend
```

检查每场景和最差切片。

---

## 18. A/B/C/F 与回退

- **A**：统一中央条带、可信 raw/无需恢复 pose、最低复杂度对齐；全部门通过。
- **B**：全部视觉和结构门通过，但使用 optimized/interpolated pose、APAP/flow、单应或 learned candidate。
- **C**：统一坐标、真实来源、owner、完整性和原子交付完整；存在 hard seam、光度或一般图像质量不足。不得有明显重影、错误位置或 required 丢失。
- **F**：第二 RGB 路径、第二 target coordinate、legacy 调用、明显重影/错位、required 丢失、provenance、owner、pose、warp、valid、资源或原子交付失败。

回退：

| 风险 | 回退 |
|---|---|
| detector/tracker 不可用 | 统一 strips 继续；降低 auditability，不改变坐标 |
| track split/merge | 不生成 lock；只降级审计 |
| depth 缺失/透明 | RGB alignment 或 identity/hard seam |
| 极近/大物体跨 strips | 相邻真实 strips +统一 seam，不创建 patch |
| pair 无安全 alignment | 完整真实 source hard seam；明显错误则 F |
| pose 优化漂移 | 整套 raw |
| optional OOD | 拒绝该候选 |
| preserve 无全 valid 矩形 | F |
| 背景退化 | 拒绝候选或回验证版本 |
| import 错 worktree | R0 失败，禁止正式交付 |

任何回退都不恢复第二条 RGB 路径。

---

## 19. 性能与治理

取消 300 秒门，记录：

- 总/阶段耗时；
- frame/source/pair/candidate；
- CPU RSS；
- GPU VRAM；
- H2D/D2H；
- provider/device；
- model SHA；
- warm-up；
- solver 收敛；
- resident strips；
- audit/crop 开销；
- CPU fallback。

同步：

- `AGENTS.md`；
- `README.md`；
- `configs/demo.yaml`；
- CLI；
- v12 schema；
- 模块职责；
- optional dependencies；
- 测试导航；
- legacy diagnostic 声明。

---

## 20. 完成定义

### 架构

- 正式 renderer=1；
- target/valid/owner 各一个；
- 所有内容统一真实中央条带；
- legacy calls=0；
- sampling model 全部在白名单；
- post overlay=0；
- 深度/track/preserve 不决定位置或 owner；
- 所有 RGB 可追溯。

### 当前强回归

- 黑色充电器无双轮廓；
- 中部纸箱、线缆和小件位置正确；
- 风扇和相邻设备无矩形硬边；
- 右侧物体和 thin structures 完整；
- 物体与货架关系正确；
- 背景无新竖带/断线；
- crop loss=0；
- 六类 audit 全过；
- §12 可视化完整。

### 泛化

- 无固定 frame/panel/depth/货架/reference 硬编码；
- detector 开关不改变坐标；
- optional backend 可关闭/拒绝/消融；
- development→冻结→validation→blind 完成；
- 明显重影、错误位置、required 丢失不能以 C 掩盖；
- 无 300 秒门但资源完整。

---

## 21. 推荐执行顺序

```text
R0 锁定实际代码、环境和黄金文件
→ R1 切断第二路径，跑通全内容真实中央条带
→ R2 接入/冻结背景 ordered seam 与光度
→ R3 加入 preserve、attachment 和只读结构审计
→ R4 pair-local auto alignment
→ R5 有界 pose 优化
→ R6 v12 硬门与标准可视化
→ R7 阈值冻结与跨场景盲测
```

R1 是最关键因果验证。

如果 R1 不改善，优先检查：

1. import/CLI 是否仍指向旧 worktree；
2. legacy overlay/owner interval 是否仍存在；
3. 是否仍使用 12-panel inspection；
4. owner/provenance 是否仍有双 source；
5. ScanPath、RGB scale 或 raw pose 是否错误。

不得在根因未闭环时用复杂模型掩盖结构问题。
